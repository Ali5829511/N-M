# نظام التعرف الآلي على لوحات السيارات
## نظام N-M لإدارة الإسكان

---

## 📋 نظرة عامة

تم إضافة نظام متقدم للتعرف الآلي على لوحات السيارات إلى نظام N-M لإدارة الإسكان. يدعم النظام طريقتين للتعرف على اللوحات:

1. **نظام المحاكاة (Mock System)** - مجاني وللاختبار
2. **Plate Recognizer API** - نظام احترافي حقيقي

---

## 🎯 المميزات الرئيسية

### ✅ نظام المحاكاة (Mock System)
- **مجاني بالكامل** - لا يحتاج إلى API Key
- **توليد أرقام لوحات سعودية واقعية** - بتنسيق صحيح
- **بيانات عشوائية للاختبار** - مع نسبة ثقة واقعية (85-99%)
- **سريع ولا يحتاج اتصال بالإنترنت**

### ✅ Plate Recognizer API
- **تعرف حقيقي على اللوحات** - باستخدام الذكاء الاصطناعي
- **دقة عالية جداً** - يصل إلى 99%
- **يدعم اللوحات السعودية** - وجميع دول الخليج
- **معلومات إضافية** - نوع المركبة، المنطقة، إلخ

---

## 🔧 التكوين والإعداد

### 1. الوصول إلى صفحة الإعدادات

يمكن الوصول إلى صفحة إعدادات API من خلال:
- **الرابط المباشر**: `pages/api_settings.html`
- **من لوحة التحكم**: قسم الإعدادات

### 2. اختيار النظام

#### نظام المحاكاة (افتراضي)
```
1. افتح صفحة api_settings.html
2. اختر "نظام المحاكاة (Mock System) - مجاني"
3. اضغط "حفظ الإعدادات"
4. جاهز للاستخدام!
```

#### Plate Recognizer API
```
1. احصل على API Token من: https://app.platerecognizer.com/start/
2. افتح صفحة api_settings.html
3. اختر "Plate Recognizer API - احترافي"
4. أدخل API Token الخاص بك
5. اختر المنطقة: السعودية (SA)
6. اضغط "اختبار الاتصال" للتحقق
7. اضغط "حفظ الإعدادات"
```

### 3. إعداد API Token

```
API Token: احصل على token من https://app.platerecognizer.com/start/
API Endpoint: https://api.platerecognizer.com/v1/plate-reader/
المنطقة: السعودية (SA)
```

**ملاحظة:** لا تشارك API Token الخاص بك مع أحد. احفظه في متغيرات البيئة (.env file).

---

## 💻 الاستخدام في النظام

### صفحة إدارة الملصقات

تم دمج نظام التعرف على اللوحات في صفحة `enhanced_stickers_management.html`:

#### 1. فتح نموذج إضافة ملصق جديد
```
- اذهب إلى صفحة "إدارة الملصقات"
- اضغط على "إضافة ملصق جديد"
```

#### 2. استخدام ميزة مسح اللوحة
```
- في حقل "رقم اللوحة"
- اضغط على زر "مسح اللوحة" 🎥
- سيتم فتح نافذة منبثقة
```

#### 3. رفع صورة اللوحة
```
- اضغط على منطقة الرفع
- أو اسحب الصورة إلى المنطقة
- انتظر التعرف التلقائي (1-3 ثواني)
```

#### 4. استخدام النتيجة
```
- سيتم عرض رقم اللوحة المتعرف عليه
- مع نسبة الثقة والتفاصيل
- اضغط "استخدام رقم اللوحة"
- سيتم ملء الحقل تلقائياً
```

---

## 📁 الملفات المضافة

### 1. نظام التعرف - JavaScript
```
js/plate-recognition.js
```
**الوظائف الرئيسية:**
- `PlateRecognitionSystem` - الكلاس الرئيسي
- `recognizePlate(imageFile)` - التعرف على لوحة من صورة
- `mockRecognition(imageFile)` - نظام المحاكاة
- `apiRecognition(imageFile)` - نظام API الحقيقي
- `generateSaudiPlateNumber()` - توليد رقم لوحة سعودي
- `formatResultHTML(result)` - تنسيق النتائج للعرض

### 2. صفحة الإعدادات
```
pages/api_settings.html
```
**المميزات:**
- اختيار النظام (Mock أو API)
- إدخال وحفظ API Token
- اختيار المنطقة الجغرافية
- اختبار الاتصال بـ API
- عرض حالة الاتصال

### 3. التعديلات على صفحة الملصقات
```
pages/enhanced_stickers_management.html
```
**التحديثات:**
- إضافة زر "مسح اللوحة" بجانب حقل رقم اللوحة
- نافذة منبثقة احترافية للتعرف
- تكامل كامل مع نظام التعرف
- تصميم متجاوب (Mobile, Tablet, Desktop)

---

## 🎨 التصميم والواجهة

### الألوان المستخدمة
```css
الأزرق الملكي: #2C3E50
الذهبي: #C9A961
الأخضر (نجاح): #27ae60
الأحمر (خطأ): #e74c3c
الرمادي: #6c757d
```

### النافذة المنبثقة
- **خلفية شفافة** مع تأثير blur
- **تصميم احترافي** مع أيقونات Font Awesome
- **متجاوب** على جميع الأجهزة
- **رسوم متحركة** سلسة

### زر مسح اللوحة
```css
.btn-scan-plate {
    background: linear-gradient(135deg, #C9A961 0%, #b8860b 100%);
    color: white;
    border: none;
    padding: 10px 15px;
    border-radius: 8px;
    cursor: pointer;
    font-weight: bold;
    transition: all 0.3s;
}
```

---

## 🔄 سير العمل (Workflow)

### 1. تحميل الصفحة
```javascript
window.addEventListener('DOMContentLoaded', function() {
    // تحميل نظام التعرف على اللوحات
    window.plateRecognitionSystem = new PlateRecognitionSystem();
    
    // التحقق من الإعدادات المحفوظة
    const systemInfo = window.plateRecognitionSystem.getSystemInfo();
    console.log('النظام النشط:', systemInfo.systemName);
});
```

### 2. فتح نافذة المسح
```javascript
function openPlateScanner() {
    // فتح النافذة المنبثقة
    document.getElementById('plateScannerModal').style.display = 'flex';
    
    // عرض معلومات النظام
    const systemInfo = window.plateRecognitionSystem.getSystemInfo();
    // عرض حالة التكوين
}
```

### 3. معالجة الصورة
```javascript
async function handlePlateImage(input) {
    const file = input.files[0];
    
    // عرض رسالة تحميل
    showLoadingMessage();
    
    try {
        // التعرف على اللوحة
        const result = await window.plateRecognitionSystem.recognizePlate(file);
        
        if (result.success) {
            // عرض النتيجة
            displayResult(result);
        } else {
            // عرض رسالة خطأ
            displayError(result.error);
        }
    } catch (error) {
        // معالجة الأخطاء
        handleError(error);
    }
}
```

### 4. استخدام النتيجة
```javascript
function usePlateNumber() {
    if (recognizedPlateNumber) {
        // ملء حقل رقم اللوحة
        document.getElementById('plateNumber').value = recognizedPlateNumber;
        
        // إغلاق النافذة
        closePlateScanner();
        
        // إظهار رسالة نجاح
        alert('✅ تم إدخال رقم اللوحة بنجاح!');
    }
}
```

---

## 📊 أمثلة على النتائج

### نظام المحاكاة
```json
{
    "success": true,
    "system": "mock",
    "plate": "ر ق ل 1234",
    "confidence": 0.92,
    "region": "SA",
    "vehicleType": "سيارة خاصة",
    "timestamp": "2025-01-23T10:30:00.000Z",
    "imageSize": 245678,
    "imageName": "plate_image.jpg"
}
```

### Plate Recognizer API
```json
{
    "success": true,
    "system": "platerecognizer",
    "plate": "ر ق ل 5678",
    "confidence": 0.98,
    "region": "SA",
    "vehicleType": "sedan",
    "timestamp": "2025-01-23T10:30:00.000Z",
    "imageSize": 245678,
    "imageName": "plate_image.jpg",
    "rawData": { /* بيانات API الكاملة */ }
}
```

### حالة الفشل
```json
{
    "success": false,
    "system": "platerecognizer",
    "error": "لم يتم العثور على لوحة في الصورة",
    "timestamp": "2025-01-23T10:30:00.000Z"
}
```

---

## 🔒 الأمان والخصوصية

### تخزين البيانات
- **API Token** يتم تخزينه في `localStorage` المتصفح
- **لا يتم إرسال البيانات** إلى أي خادم خارجي (في نظام المحاكاة)
- **الصور** لا يتم حفظها أو تخزينها

### الصلاحيات
- **يحتاج المستخدم** صلاحية الوصول إلى صفحة الإعدادات
- **API Token** محمي ولا يظهر في الكود المصدري
- **التشفير** يتم عبر HTTPS عند استخدام API

---

## 🧪 الاختبار

### اختبار نظام المحاكاة
```
1. افتح صفحة api_settings.html
2. اختر "نظام المحاكاة"
3. احفظ الإعدادات
4. اذهب إلى صفحة الملصقات
5. اضغط "مسح اللوحة"
6. ارفع أي صورة
7. سيتم توليد رقم لوحة عشوائي
```

### اختبار Plate Recognizer API
```
1. افتح صفحة api_settings.html
2. اختر "Plate Recognizer API"
3. أدخل API Token
4. اضغط "اختبار الاتصال"
5. إذا نجح، احفظ الإعدادات
6. اذهب إلى صفحة الملصقات
7. اضغط "مسح اللوحة"
8. ارفع صورة لوحة حقيقية
9. سيتم التعرف على اللوحة
```

---

## 🐛 معالجة الأخطاء

### الأخطاء الشائعة

#### 1. API Token غير صحيح
```
الخطأ: "فشل في الاتصال بـ API"
الحل: تحقق من صحة API Token في صفحة الإعدادات
```

#### 2. لم يتم العثور على لوحة
```
الخطأ: "لم يتم العثور على لوحة في الصورة"
الحل: تأكد من وضوح الصورة ووجود لوحة واضحة
```

#### 3. انتهت صلاحية API
```
الخطأ: "تجاوزت الحد المسموح من الاستدعاءات"
الحل: انتقل إلى نظام المحاكاة أو احصل على API جديد
```

#### 4. مشكلة في الاتصال
```
الخطأ: "خطأ في الاتصال: Network Error"
الحل: تحقق من اتصال الإنترنت
```

---

## 📱 التوافق

### المتصفحات المدعومة
- ✅ Chrome 90+
- ✅ Firefox 88+
- ✅ Safari 14+
- ✅ Edge 90+

### الأجهزة المدعومة
- ✅ Desktop (Windows, Mac, Linux)
- ✅ Tablet (iPad, Android Tablets)
- ✅ Mobile (iPhone, Android Phones)

### أنواع الصور المدعومة
- ✅ JPG/JPEG
- ✅ PNG
- ✅ WebP
- ✅ BMP

---

## 🚀 التحسينات المستقبلية

### المخطط لها
1. **دعم الكاميرا المباشرة** - التقاط صورة مباشرة من الكاميرا
2. **معالجة دفعية** - التعرف على عدة لوحات دفعة واحدة
3. **سجل التعرف** - حفظ تاريخ اللوحات المتعرف عليها
4. **تحسين الصور** - معالجة الصور تلقائياً قبل التعرف
5. **دعم لغات إضافية** - لوحات دول أخرى

---

## 📞 الدعم والمساعدة

### الحصول على API Token
- **الموقع**: https://app.platerecognizer.com/start/
- **التسجيل**: مجاني مع 2500 استدعاء شهرياً
- **الترقية**: خطط مدفوعة لاستخدام أكبر

### التوثيق الرسمي
- **Plate Recognizer Docs**: https://docs.platerecognizer.com/
- **API Reference**: https://docs.platerecognizer.com/api-reference

### المشاكل والأسئلة
- **GitHub Issues**: https://github.com/Ali5829511/N-M/issues
- **البريد الإلكتروني**: support@n-m-system.com

---

## 📝 سجل التحديثات

### الإصدار 1.0.0 (23 يناير 2025)
- ✅ إضافة نظام التعرف على لوحات السيارات
- ✅ دعم نظام المحاكاة (Mock System)
- ✅ دعم Plate Recognizer API
- ✅ صفحة إعدادات API
- ✅ دمج في صفحة إدارة الملصقات
- ✅ نافذة منبثقة احترافية
- ✅ تصميم متجاوب كامل
- ✅ توليد أرقام لوحات سعودية واقعية
- ✅ معالجة الأخطاء الشاملة
- ✅ توثيق كامل

---

## 🎓 أمثلة الاستخدام البرمجي

### مثال 1: استخدام النظام مباشرة
```javascript
// إنشاء نسخة من النظام
const plateSystem = new PlateRecognitionSystem();

// التعرف على لوحة من صورة
const fileInput = document.getElementById('imageInput');
const file = fileInput.files[0];

const result = await plateSystem.recognizePlate(file);

if (result.success) {
    console.log('رقم اللوحة:', result.plate);
    console.log('الثقة:', result.confidence);
} else {
    console.error('خطأ:', result.error);
}
```

### مثال 2: تغيير النظام برمجياً
```javascript
// التبديل إلى نظام المحاكاة
window.plateRecognitionSystem.setSystem('mock');

// التبديل إلى Plate Recognizer API
window.plateRecognitionSystem.setSystem('platerecognizer');
window.plateRecognitionSystem.setAPIToken('your_api_token_here');
window.plateRecognitionSystem.setRegion('sa');
```

### مثال 3: الحصول على معلومات النظام
```javascript
const systemInfo = window.plateRecognitionSystem.getSystemInfo();

console.log('النظام:', systemInfo.systemName);
console.log('مكوّن:', systemInfo.isConfigured);
console.log('المنطقة:', systemInfo.region);
```

---

## 🏆 الخلاصة

تم بنجاح إضافة نظام متقدم للتعرف الآلي على لوحات السيارات إلى نظام N-M. النظام يوفر:

- ✅ **مرونة** - خيارين للتعرف (Mock و API)
- ✅ **سهولة الاستخدام** - واجهة بديهية وبسيطة
- ✅ **احترافية** - تصميم متقدم ومتجاوب
- ✅ **أمان** - حماية البيانات والخصوصية
- ✅ **قابلية التوسع** - سهل الإضافة لصفحات أخرى

النظام جاهز للاستخدام الفوري في بيئة الإنتاج!

---

**تاريخ الإنشاء**: 23 يناير 2025  
**الإصدار**: 1.0.0  
**الحالة**: ✅ مكتمل وجاهز للإنتاج  
**المطور**: فريق N-M System

---
