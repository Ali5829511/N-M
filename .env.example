# Environment Configuration for N-M Traffic Management System
# نظام إدارة المرور - ملف الإعدادات البيئية

# Server Configuration
# إعدادات الخادم
PORT=8080
HOST=0.0.0.0
NODE_ENV=development

# ParkPow API Configuration
# إعدادات ParkPow API
# Get your API token from: https://app.parkpow.com
# احصل على رمز API من: https://app.parkpow.com
PARKPOW_API_TOKEN=7c13be422713a758a42a0bc453cf3331fbf4d346
PARKPOW_API_URL=https://app.parkpow.com/api/v1
PARKPOW_WEBHOOK_URL=https://app.parkpow.com/api/v1/webhook-receiver/

# Stream Integration (for real-time data sync with ParkPow)
# تكامل Stream (للمزامنة الفورية مع ParkPow)
# See config.ini for Stream webhook configuration
# راجع config.ini لإعدادات Stream webhook
STREAM_ENABLED=false
STREAM_CONFIG_PATH=./config.ini

# Plate Recognizer FTP Configuration
# إعدادات FTP للتعرف على اللوحات
FTP_HOST=ftp.platerecognizer.com
FTP_PORT=21
FTP_PORT_FTPS=2121
FTP_PORT_SFTP=2022
FTP_USERNAME=aliayashi522
FTP_PASSWORD=708c4bbfdde0

# Email Service Configuration (EmailJS)
# إعدادات خدمة البريد الإلكتروني
# Get your keys from: https://www.emailjs.com/
EMAILJS_SERVICE_ID=your_service_id_here
EMAILJS_PUBLIC_KEY=your_public_key_here

# Security
# الأمان
# Generate a random secret key for sessions
SESSION_SECRET=your_session_secret_here

# Database Configuration (for future use)
# إعدادات قاعدة البيانات (للاستخدام المستقبلي)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=traffic_system
DB_USER=postgres
DB_PASSWORD=strong_password_here

# Plate Recognizer Snapshot API Configuration
# إعدادات Plate Recognizer Snapshot API
# Get your API token from: https://platerecognizer.com
# احصل على رمز API من: https://platerecognizer.com
PLATE_RECOGNIZER_API_TOKEN=your_api_token_here
PLATE_RECOGNIZER_API_URL=https://api.platerecognizer.com/v1/plate-reader/

# ⚠️ Storage Warning / تحذير التخزين:
# Storing images in the database will significantly increase storage requirements.
# تخزين الصور في قاعدة البيانات سيزيد بشكل كبير من متطلبات التخزين.
# Consider:
# - Regular cleanup of old data / تنظيف منتظم للبيانات القديمة
# - Database storage capacity / سعة تخزين قاعدة البيانات
# - Backup strategy / استراتيجية النسخ الاحتياطي

# ⚠️ Important Security Notes / ملاحظات أمنية مهمة:
# 1. Never commit the actual .env file to version control
#    لا تقم بإضافة ملف .env الفعلي إلى نظام التحكم بالإصدار
# 2. Keep your API tokens and secrets secure
#    احفظ رموز API والأسرار بشكل آمن
# 3. Use different tokens for development and production
#    استخدم رموز مختلفة للتطوير والإنتاج
# 4. Rotate tokens regularly
#    قم بتغيير الرموز بشكل دوري
# 5. Never share FTP credentials publicly
#    لا تشارك بيانات FTP بشكل علني


# ============================================
# Neon PostgreSQL Database Configuration
# إعدادات قاعدة البيانات Neon PostgreSQL
# ============================================

# Neon Database URL
# رابط قاعدة بيانات Neon
#
# How to get your database URL:
# كيفية الحصول على رابط قاعدة البيانات:
#
# 1. Go to Neon Console: https://console.neon.tech
#    اذهب إلى لوحة تحكم Neon
#
# 2. Select your project / اختر مشروعك
#
# 3. Click "Connection Details" / اضغط على "تفاصيل الاتصال"
#
# 4. Copy the connection string / انسخ رابط الاتصال
#
# Format: postgresql://[user]:[password]@[host]/[database]?sslmode=require
# مثال: postgresql://user:password@ep-xxxxx.us-east-2.aws.neon.tech/neondb?sslmode=require
#
DATABASE_URL=postgresql://user:password@ep-xxxxx.us-east-2.aws.neon.tech/neondb?sslmode=require

# Docker PostgreSQL Credentials (for docker-compose)
# بيانات PostgreSQL لـ Docker
POSTGRES_USER=user
POSTGRES_PASSWORD=change_this_password
POSTGRES_DB=platenet

# For Netlify deployment (automatic via Neon extension):
# للنشر على Netlify (تلقائي عبر إضافة Neon):
#
# Option 1 (Recommended): Use Neon Extension
# الخيار الأول (موصى به): استخدم إضافة Neon
# - Install from: https://app.netlify.com/projects/n-m-m/extensions/neon
# - DATABASE_URL will be added automatically
# - سيتم إضافة DATABASE_URL تلقائياً
#
# Option 2: Manual setup
# الخيار الثاني: الإعداد اليدوي
# - Add DATABASE_URL in Netlify: Site settings > Environment variables
# - أضف DATABASE_URL في Netlify من: إعدادات الموقع > المتغيرات البيئية
#
# For detailed guide, see: NETLIFY_NEON_SETUP.md
# للدليل التفصيلي، راجع: NETLIFY_NEON_SETUP.md

# ============================================
# Plate Recognizer API Configuration
# إعدادات Plate Recognizer API
# ============================================

# Plate Recognizer API Key
# مفتاح API للتعرف على اللوحات
# Get your API key from: https://app.platerecognizer.com/
# احصل على مفتاح API من: https://app.platerecognizer.com/
PLATE_API_KEY=your_plate_recognizer_api_key_here

# API Type: "snapshot" (cloud) or "sdk" (on-premise)
# نوع API: "snapshot" للخدمة السحابية أو "sdk" للنظام المحلي
# - "snapshot": Uses Plate Recognizer Snapshot API (cloud-based, requires API key)
#   يستخدم Snapshot API السحابي (يتطلب مفتاح API)
# - "sdk": Uses Plate Recognizer SDK/Server (on-premise, self-hosted via Docker)
#   يستخدم SDK المحلي (يتم استضافته محلياً عبر Docker)
# Default: snapshot
# الافتراضي: snapshot
PLATE_API_TYPE=snapshot

# Snapshot API Endpoint (for PLATE_API_TYPE=snapshot)
# نقطة نهاية Snapshot API (لـ PLATE_API_TYPE=snapshot)
# Official cloud endpoint from Plate Recognizer documentation
# النقطة السحابية الرسمية من توثيق Plate Recognizer
# Reference: https://guides.platerecognizer.com/docs/snapshot/api-reference/
SNAPSHOT_API_URL=https://api.platerecognizer.com/v1/plate-reader/

# SDK/Server API Endpoint (for PLATE_API_TYPE=sdk)
# نقطة نهاية SDK/Server API (لـ PLATE_API_TYPE=sdk)
# For self-hosted Plate Recognizer SDK/Server
# للنظام المحلي المستضاف ذاتياً
# Default Docker setup uses: http://localhost:8080/v1/plate-reader/
# الإعداد الافتراضي لـ Docker يستخدم: http://localhost:8080/v1/plate-reader/
# Reference: https://guides.platerecognizer.com/docs/tech-references/server/
SDK_API_URL=http://localhost:8080/v1/plate-reader/

# SDK License Token (required for SDK/Server, not needed for Snapshot API)
# رمز ترخيص SDK (مطلوب لـ SDK/Server، غير مطلوب لـ Snapshot API)
# Get your SDK license from: https://platerecognizer.com/
# احصل على ترخيص SDK من: https://platerecognizer.com/
# Note: This is different from PLATE_API_KEY
# ملاحظة: هذا يختلف عن PLATE_API_KEY
SDK_LICENSE_TOKEN=your_sdk_license_token_here

# ============================================
# Image Storage Configuration
# إعدادات تخزين الصور
# ============================================

# Storage mode: "s3" or "db"
# وضع التخزين: "s3" للتخزين في S3، أو "db" للتخزين في قاعدة البيانات
# Default: s3 (recommended for production)
# الافتراضي: s3 (موصى به للإنتاج)
STORE_IMAGES=s3

# ============================================
# AWS S3 Configuration (required when STORE_IMAGES=s3)
# إعدادات AWS S3 (مطلوبة عند استخدام STORE_IMAGES=s3)
# ============================================

# S3 Bucket Name
# اسم حاوية S3
S3_BUCKET=your-bucket-name-here

# AWS Region
# منطقة AWS
AWS_REGION=us-east-1

# AWS Access Credentials
# بيانات الوصول إلى AWS
# ⚠️ NEVER commit these to version control
# ⚠️ لا تضف هذه المفاتيح أبداً إلى نظام التحكم بالإصدار
AWS_ACCESS_KEY_ID=your_aws_access_key_id_here
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key_here

# ============================================
# MinIO Configuration (alternative to AWS S3)
# إعدادات MinIO (بديل لـ AWS S3)
# ============================================
# If using MinIO instead of AWS S3, use these settings:
# إذا كنت تستخدم MinIO بدلاً من AWS S3، استخدم هذه الإعدادات:
#
# S3_BUCKET=your-bucket-name
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=minioadmin
# AWS_SECRET_ACCESS_KEY=minioadmin
# AWS_ENDPOINT_URL=http://localhost:9000  # MinIO endpoint
#
# Note: AWS_ENDPOINT_URL is now supported! Uncomment to use MinIO or other S3-compatible storage
# ملاحظة: AWS_ENDPOINT_URL مدعوم الآن! قم بإلغاء التعليق لاستخدام MinIO أو تخزين متوافق مع S3

# ============================================
# Security Notes / ملاحظات الأمان
# ============================================
# 
# ⚠️ IMPORTANT - لا تضف المفاتيح الحقيقية إلى Git:
# 1. Copy .env.example to .env: cp .env.example .env
#    انسخ .env.example إلى .env
# 2. Fill in your actual credentials in .env
#    املأ بيانات الاعتماد الحقيقية في .env
# 3. .env is in .gitignore and will NOT be committed
#    .env في .gitignore ولن يتم إضافته إلى Git
# 4. For GitHub Actions/CI, use GitHub Secrets
#    لـ GitHub Actions/CI، استخدم GitHub Secrets
# 5. For production, use environment variables or secrets management
#    للإنتاج، استخدم متغيرات البيئة أو إدارة الأسرار

