# 🚗 توثيق نظام قاعدة بيانات السيارات

## نظرة عامة

نظام شامل لإدارة سيارات الساكنين مع تحليل وتخزين الصور، مدمج بالكامل مع نظام التعرف على لوحات السيارات ونظام الملصقات.

---

## 📋 المحتويات

1. [المميزات الرئيسية](#المميزات-الرئيسية)
2. [الملفات المضافة](#الملفات-المضافة)
3. [هيكل قاعدة البيانات](#هيكل-قاعدة-البيانات)
4. [الوظائف المتاحة](#الوظائف-المتاحة)
5. [التكامل مع الأنظمة الأخرى](#التكامل-مع-الأنظمة-الأخرى)
6. [دليل الاستخدام](#دليل-الاستخدام)
7. [أمثلة برمجية](#أمثلة-برمجية)

---

## 🎯 المميزات الرئيسية

### 1. **قاعدة بيانات شاملة**
- ✅ تخزين معلومات السيارة الكاملة (رقم اللوحة، النوع، الماركة، الموديل، السنة، اللون)
- ✅ ربط السيارات بالساكنين (رقم الهوية، الاسم)
- ✅ ربط السيارات بالوحدات السكنية (نوع الوحدة، المبنى، الشقة)
- ✅ ربط السيارات بالملصقات
- ✅ حالة السيارة (نشط / غير نشط)
- ✅ ملاحظات وتواريخ

### 2. **نظام تخزين الصور**
- ✅ رفع صور متعددة لكل سيارة
- ✅ تصنيف الصور (لوحة، واجهة، خلفية، جانب، أخرى)
- ✅ تخزين الصور بصيغة Base64
- ✅ معاينة وحذف الصور
- ✅ ربط الصور بالسيارة

### 3. **سجل التاريخ**
- ✅ تسجيل جميع العمليات (إضافة، تعديل، حذف)
- ✅ تسجيل إضافة الصور
- ✅ طوابع زمنية لكل عملية
- ✅ عرض سجل كامل لكل سيارة

### 4. **البحث والتصفية**
- ✅ البحث بالنص الحر (رقم لوحة، نوع، ماركة، لون، ساكن)
- ✅ التصفية حسب الحالة (نشط / غير نشط)
- ✅ التصفية حسب نوع الوحدة
- ✅ ترقيم الصفحات

### 5. **الإحصائيات**
- ✅ إجمالي السيارات
- ✅ السيارات النشطة / غير النشطة
- ✅ السيارات التي لديها صور
- ✅ السيارات التي لديها ملصقات
- ✅ التوزيع حسب نوع الوحدة
- ✅ التوزيع حسب نوع المركبة
- ✅ التوزيع حسب اللون والسنة

### 6. **الاستيراد والتصدير**
- ✅ استيراد تلقائي من بيانات الملصقات
- ✅ تصدير البيانات إلى JSON
- ✅ استيراد البيانات من JSON
- ✅ نسخ احتياطي كامل

### 7. **التكامل مع الأنظمة الأخرى**
- ✅ التكامل مع نظام التعرف على اللوحات
- ✅ التكامل مع نظام الملصقات
- ✅ الملء التلقائي للبيانات
- ✅ التحديث التلقائي عند حفظ الملصقات

---

## 📁 الملفات المضافة

### 1. **js/vehicles-database.js**
ملف JavaScript الرئيسي لنظام قاعدة البيانات.

**الحجم:** ~15 KB  
**الوظائف:** 25+ وظيفة  
**التخزين:** LocalStorage

### 2. **pages/vehicles_management.html**
واجهة إدارة السيارات الكاملة.

**الحجم:** ~38 KB  
**المميزات:**
- واجهة مستخدم احترافية
- جدول عرض السيارات
- نماذج إضافة وتعديل
- نافذة عرض التفاصيل
- رفع وإدارة الصور
- إحصائيات مباشرة

### 3. **التحديثات على الملفات الموجودة**

#### pages/enhanced_stickers_management.html
- إضافة `<script src="../js/vehicles-database.js"></script>`
- تحديث وظيفة `usePlateNumber()` للملء التلقائي من قاعدة البيانات
- إضافة وظيفة `saveSticker()` المحدثة لإضافة/تحديث السيارة تلقائياً

---

## 🗄️ هيكل قاعدة البيانات

### 1. **جدول السيارات (vehicles_database)**

```javascript
{
  id: "VEH-1234567890-abc123",           // معرف فريد
  plateNumber: "أ ب ج 1234",            // رقم اللوحة
  vehicleType: "سيدان",                 // نوع المركبة
  vehicleBrand: "تويوتا",               // الماركة
  vehicleModel: "كامري",                // الموديل
  vehicleYear: "2023",                  // سنة الصنع
  vehicleColor: "أبيض",                 // اللون
  nationalId: "1234567890",             // رقم الهوية
  residentName: "محمد أحمد",            // اسم الساكن
  unitType: "شقة",                      // نوع الوحدة
  building: "A1",                       // رقم المبنى
  apartment: "101",                     // رقم الشقة
  status: "نشط",                        // الحالة
  registrationDate: "2024-01-15",       // تاريخ التسجيل
  notes: "ملاحظات...",                  // ملاحظات
  stickerId: "STK-123",                 // معرف الملصق
  createdAt: "2024-01-15T10:30:00",     // تاريخ الإنشاء
  updatedAt: "2024-01-15T10:30:00"      // تاريخ التحديث
}
```

### 2. **جدول الصور (vehicles_images)**

```javascript
{
  "VEH-1234567890-abc123": [
    {
      id: "IMG-1234567890-xyz789",       // معرف الصورة
      vehicleId: "VEH-1234567890-abc123", // معرف السيارة
      imageData: "data:image/jpeg;base64,...", // بيانات الصورة
      imageType: "plate",                // نوع الصورة
      uploadedAt: "2024-01-15T10:35:00"  // تاريخ الرفع
    }
  ]
}
```

**أنواع الصور المدعومة:**
- `plate` - صورة اللوحة
- `front` - صورة الواجهة
- `back` - صورة الخلفية
- `side` - صورة الجانب
- `other` - أخرى

### 3. **جدول التاريخ (vehicles_history)**

```javascript
[
  {
    vehicleId: "VEH-1234567890-abc123",  // معرف السيارة
    action: "إضافة سيارة",               // نوع العملية
    details: "تم إضافة سيارة برقم...",   // تفاصيل
    timestamp: "2024-01-15T10:30:00"     // الطابع الزمني
  }
]
```

---

## ⚙️ الوظائف المتاحة

### 1. **إدارة السيارات**

#### `getAllVehicles()`
الحصول على جميع السيارات.

```javascript
const vehicles = window.vehiclesDB.getAllVehicles();
console.log('عدد السيارات:', vehicles.length);
```

#### `getVehicleByPlate(plateNumber)`
الحصول على سيارة بواسطة رقم اللوحة.

```javascript
const vehicle = window.vehiclesDB.getVehicleByPlate('أ ب ج 1234');
if (vehicle) {
  console.log('السيارة موجودة:', vehicle.vehicleBrand, vehicle.vehicleModel);
}
```

#### `getVehiclesByResident(nationalId)`
الحصول على سيارات ساكن معين.

```javascript
const vehicles = window.vehiclesDB.getVehiclesByResident('1234567890');
console.log('عدد سيارات الساكن:', vehicles.length);
```

#### `addVehicle(vehicleData)`
إضافة سيارة جديدة.

```javascript
const result = window.vehiclesDB.addVehicle({
  plateNumber: 'أ ب ج 1234',
  vehicleType: 'سيدان',
  vehicleBrand: 'تويوتا',
  vehicleModel: 'كامري',
  vehicleYear: '2023',
  vehicleColor: 'أبيض',
  nationalId: '1234567890',
  residentName: 'محمد أحمد',
  unitType: 'شقة',
  building: 'A1',
  apartment: '101',
  status: 'نشط'
});

if (result.success) {
  console.log('تم إضافة السيارة:', result.vehicle.id);
} else {
  console.error('خطأ:', result.error);
}
```

#### `updateVehicle(vehicleId, updates)`
تحديث بيانات سيارة.

```javascript
const result = window.vehiclesDB.updateVehicle('VEH-123', {
  vehicleColor: 'أسود',
  status: 'غير نشط'
});

if (result.success) {
  console.log('تم التحديث بنجاح');
}
```

#### `deleteVehicle(vehicleId)`
حذف سيارة.

```javascript
const result = window.vehiclesDB.deleteVehicle('VEH-123');
if (result.success) {
  console.log('تم حذف السيارة');
}
```

### 2. **إدارة الصور**

#### `addVehicleImage(vehicleId, imageData, imageType)`
إضافة صورة لسيارة.

```javascript
const result = window.vehiclesDB.addVehicleImage(
  'VEH-123',
  'data:image/jpeg;base64,...',
  'plate'
);

if (result.success) {
  console.log('تم إضافة الصورة:', result.image.id);
}
```

#### `getVehicleImages(vehicleId)`
الحصول على صور سيارة.

```javascript
const images = window.vehiclesDB.getVehicleImages('VEH-123');
console.log('عدد الصور:', images.length);
```

#### `deleteImage(vehicleId, imageId)`
حذف صورة.

```javascript
const result = window.vehiclesDB.deleteImage('VEH-123', 'IMG-456');
```

### 3. **البحث والتصفية**

#### `searchVehicles(query)`
البحث عن سيارات.

```javascript
const results = window.vehiclesDB.searchVehicles('تويوتا');
console.log('نتائج البحث:', results.length);
```

### 4. **الإحصائيات**

#### `getStatistics()`
الحصول على إحصائيات شاملة.

```javascript
const stats = window.vehiclesDB.getStatistics();
console.log('إجمالي السيارات:', stats.total);
console.log('السيارات النشطة:', stats.active);
console.log('السيارات بصور:', stats.withImages);
console.log('التوزيع حسب النوع:', stats.byVehicleType);
```

### 5. **الاستيراد والتصدير**

#### `importFromStickers()`
استيراد السيارات من بيانات الملصقات.

```javascript
const result = window.vehiclesDB.importFromStickers();
console.log('تم استيراد:', result.imported);
console.log('تم التخطي:', result.skipped);
```

#### `exportToJSON()`
تصدير البيانات إلى JSON.

```javascript
const jsonData = window.vehiclesDB.exportToJSON();
// حفظ أو تنزيل jsonData
```

#### `importFromJSON(jsonData)`
استيراد البيانات من JSON.

```javascript
const result = window.vehiclesDB.importFromJSON(jsonData);
if (result.success) {
  console.log('تم الاستيراد بنجاح');
}
```

### 6. **سجل التاريخ**

#### `getVehicleHistory(vehicleId)`
الحصول على تاريخ سيارة.

```javascript
const history = window.vehiclesDB.getVehicleHistory('VEH-123');
history.forEach(entry => {
  console.log(entry.action, '-', entry.details);
});
```

---

## 🔗 التكامل مع الأنظمة الأخرى

### 1. **التكامل مع نظام التعرف على اللوحات**

عند استخدام نظام التعرف على اللوحات في صفحة الملصقات:

1. **التعرف على اللوحة** → يتم البحث في قاعدة بيانات السيارات
2. **إذا وُجدت السيارة** → يتم ملء جميع الحقول تلقائياً
3. **إذا لم تُوجد** → يتم إدخال رقم اللوحة فقط

```javascript
// في usePlateNumber()
const vehicle = window.vehiclesDB.getVehicleByPlate(recognizedPlateNumber);
if (vehicle) {
  // ملء البيانات من قاعدة البيانات
  document.getElementById('vehicleType').value = vehicle.vehicleType;
  document.getElementById('nationalId').value = vehicle.nationalId;
  // ... إلخ
}
```

### 2. **التكامل مع نظام الملصقات**

عند حفظ ملصق جديد:

1. **التحقق من وجود السيارة** في قاعدة البيانات
2. **إذا وُجدت** → تحديث بياناتها
3. **إذا لم تُوجد** → إضافتها تلقائياً

```javascript
// في saveSticker()
const plateNumber = document.getElementById('plateNumber').value;
const existingVehicle = window.vehiclesDB.getVehicleByPlate(plateNumber);

if (existingVehicle) {
  // تحديث
  window.vehiclesDB.updateVehicle(existingVehicle.id, vehicleData);
} else {
  // إضافة
  window.vehiclesDB.addVehicle(vehicleData);
}
```

### 3. **مزامنة البيانات**

- ✅ **تلقائية:** عند حفظ ملصق، يتم تحديث/إضافة السيارة تلقائياً
- ✅ **يدوية:** يمكن استيراد جميع السيارات من الملصقات دفعة واحدة
- ✅ **ثنائية الاتجاه:** التحديثات في أي نظام تنعكس على الآخر

---

## 📖 دليل الاستخدام

### للمستخدم النهائي

#### 1. **الوصول إلى صفحة إدارة السيارات**

افتح الرابط:
```
https://hilarious-narwhal-1a5720.netlify.app/pages/vehicles_management.html
```

#### 2. **عرض السيارات**

- ستظهر جميع السيارات المسجلة في جدول
- يمكنك رؤية الصورة، رقم اللوحة، النوع، الماركة، اللون، الساكن، الوحدة، والحالة

#### 3. **البحث والتصفية**

- **البحث:** اكتب في مربع البحث (رقم لوحة، نوع، لون، اسم ساكن...)
- **التصفية:** اختر الحالة أو نوع الوحدة من القوائم المنسدلة

#### 4. **إضافة سيارة جديدة**

1. اضغط "➕ إضافة سيارة جديدة"
2. املأ البيانات المطلوبة:
   - رقم اللوحة (إجباري)
   - نوع المركبة (إجباري)
   - الماركة والموديل (اختياري)
   - السنة واللون (اختياري)
   - بيانات الساكن والوحدة (اختياري)
3. ارفع صور السيارة (اختياري):
   - اضغط على منطقة الرفع
   - اختر الصور
   - حدد نوع كل صورة
4. اضغط "💾 حفظ"

#### 5. **تعديل سيارة**

1. اضغط زر "✏️" بجانب السيارة
2. عدّل البيانات المطلوبة
3. أضف أو احذف صور
4. اضغط "💾 حفظ"

#### 6. **عرض تفاصيل سيارة**

1. اضغط زر "👁️" أو على صورة السيارة
2. ستظهر نافذة بجميع التفاصيل:
   - معلومات السيارة الكاملة
   - جميع الصور
   - سجل التاريخ

#### 7. **حذف سيارة**

1. اضغط زر "🗑️" بجانب السيارة
2. أكّد الحذف

#### 8. **استيراد من الملصقات**

1. اضغط "📥 استيراد من الملصقات"
2. سيتم استيراد جميع السيارات من بيانات الملصقات
3. السيارات الموجودة مسبقاً سيتم تخطيها

### للمطور

#### 1. **التضمين في صفحة HTML**

```html
<!-- إضافة السكريبت -->
<script src="../js/vehicles-database.js"></script>

<!-- الاستخدام -->
<script>
  // النظام جاهز تلقائياً
  const vehicles = window.vehiclesDB.getAllVehicles();
  console.log('عدد السيارات:', vehicles.length);
</script>
```

#### 2. **إنشاء واجهة مخصصة**

```javascript
// الحصول على البيانات
const vehicles = window.vehiclesDB.getAllVehicles();

// عرض في جدول
const tbody = document.getElementById('vehiclesTable');
tbody.innerHTML = vehicles.map(v => `
  <tr>
    <td>${v.plateNumber}</td>
    <td>${v.vehicleType}</td>
    <td>${v.vehicleBrand} ${v.vehicleModel}</td>
  </tr>
`).join('');
```

#### 3. **التعامل مع الصور**

```javascript
// رفع صورة
const fileInput = document.getElementById('imageInput');
fileInput.addEventListener('change', function(e) {
  const file = e.target.files[0];
  const reader = new FileReader();
  
  reader.onload = function(event) {
    const imageData = event.target.result;
    
    window.vehiclesDB.addVehicleImage(
      vehicleId,
      imageData,
      'plate'
    );
  };
  
  reader.readAsDataURL(file);
});
```

---

## 💡 أمثلة برمجية

### مثال 1: إضافة سيارة مع صورة

```javascript
// إضافة السيارة
const vehicleResult = window.vehiclesDB.addVehicle({
  plateNumber: 'أ ب ج 1234',
  vehicleType: 'سيدان',
  vehicleBrand: 'تويوتا',
  vehicleModel: 'كامري',
  vehicleYear: '2023',
  vehicleColor: 'أبيض',
  nationalId: '1234567890',
  residentName: 'محمد أحمد',
  unitType: 'شقة',
  building: 'A1',
  apartment: '101'
});

if (vehicleResult.success) {
  const vehicleId = vehicleResult.vehicle.id;
  
  // إضافة صورة
  const imageResult = window.vehiclesDB.addVehicleImage(
    vehicleId,
    'data:image/jpeg;base64,/9j/4AAQSkZJRg...',
    'plate'
  );
  
  if (imageResult.success) {
    console.log('✅ تم إضافة السيارة والصورة بنجاح!');
  }
}
```

### مثال 2: البحث والتصفية

```javascript
// البحث عن سيارات تويوتا
const toyotas = window.vehiclesDB.searchVehicles('تويوتا');

// التصفية حسب الحالة
const activeVehicles = toyotas.filter(v => v.status === 'نشط');

// التصفية حسب نوع الوحدة
const apartmentVehicles = activeVehicles.filter(v => v.unitType === 'شقة');

console.log('عدد سيارات تويوتا النشطة في الشقق:', apartmentVehicles.length);
```

### مثال 3: عرض إحصائيات

```javascript
const stats = window.vehiclesDB.getStatistics();

console.log('📊 إحصائيات السيارات:');
console.log('━━━━━━━━━━━━━━━━━━━━━━');
console.log(`إجمالي السيارات: ${stats.total}`);
console.log(`السيارات النشطة: ${stats.active}`);
console.log(`السيارات غير النشطة: ${stats.inactive}`);
console.log(`السيارات بصور: ${stats.withImages}`);
console.log(`السيارات بملصقات: ${stats.withStickers}`);
console.log('');
console.log('التوزيع حسب النوع:');
Object.entries(stats.byVehicleType).forEach(([type, count]) => {
  console.log(`  ${type}: ${count}`);
});
```

### مثال 4: تصدير واستيراد

```javascript
// تصدير البيانات
const jsonData = window.vehiclesDB.exportToJSON();

// حفظ في ملف
const blob = new Blob([jsonData], { type: 'application/json' });
const url = URL.createObjectURL(blob);
const a = document.createElement('a');
a.href = url;
a.download = 'vehicles_backup.json';
a.click();

// استيراد البيانات
const fileInput = document.getElementById('importFile');
fileInput.addEventListener('change', function(e) {
  const file = e.target.files[0];
  const reader = new FileReader();
  
  reader.onload = function(event) {
    const jsonData = event.target.result;
    const result = window.vehiclesDB.importFromJSON(jsonData);
    
    if (result.success) {
      alert('✅ تم الاستيراد بنجاح!');
      location.reload();
    }
  };
  
  reader.readAsText(file);
});
```

### مثال 5: عرض سجل التاريخ

```javascript
const vehicleId = 'VEH-123';
const history = window.vehiclesDB.getVehicleHistory(vehicleId);

console.log('📜 سجل التاريخ:');
console.log('━━━━━━━━━━━━━━━━━━━━━━');

history.forEach(entry => {
  const date = new Date(entry.timestamp).toLocaleString('ar-SA');
  console.log(`${date} - ${entry.action}`);
  console.log(`  ${entry.details}`);
  console.log('');
});
```

---

## 🔧 الإعدادات والتخصيص

### تغيير عدد العناصر في الصفحة

في ملف `vehicles_management.html`:

```javascript
const itemsPerPage = 10; // غيّر هذا الرقم
```

### تخصيص الألوان

في قسم `<style>` في `vehicles_management.html`:

```css
:root {
  --primary-color: #667eea;
  --secondary-color: #764ba2;
  --success-color: #11998e;
  --danger-color: #eb3349;
}
```

### إضافة حقول مخصصة

1. في `vehicles-database.js`، عدّل دالة `addVehicle()`:

```javascript
const vehicle = {
  // ... الحقول الموجودة
  customField1: vehicleData.customField1 || '',
  customField2: vehicleData.customField2 || ''
};
```

2. في `vehicles_management.html`، أضف الحقول في النموذج:

```html
<div class="form-group">
  <label for="customField1">حقل مخصص 1</label>
  <input type="text" id="customField1">
</div>
```

---

## 🐛 استكشاف الأخطاء

### المشكلة: البيانات لا تُحفظ

**الحل:**
- تأكد من تفعيل LocalStorage في المتصفح
- تحقق من وجود مساحة كافية (الحد الأقصى ~10MB)
- افتح Console وتحقق من الأخطاء

### المشكلة: الصور لا تظهر

**الحل:**
- تأكد من أن الصور بصيغة Base64
- تحقق من حجم الصورة (يُفضل أقل من 1MB)
- جرّب ضغط الصورة قبل الرفع

### المشكلة: الاستيراد من الملصقات لا يعمل

**الحل:**
- تأكد من وجود بيانات في `localStorage.getItem('stickers_data')`
- تحقق من تنسيق البيانات
- افتح Console وتحقق من رسائل الخطأ

### المشكلة: البحث بطيء

**الحل:**
- قلل عدد السيارات المعروضة في الصفحة
- استخدم التصفية قبل البحث
- احذف السيارات القديمة أو غير المستخدمة

---

## 📊 حدود النظام

### LocalStorage
- **الحد الأقصى:** ~10MB
- **عدد السيارات:** ~1000-2000 (حسب عدد الصور)
- **عدد الصور:** ~50-100 صورة إجمالاً (حسب الحجم)

### الأداء
- **البحث:** سريع حتى 5000 سيارة
- **التصفية:** سريعة حتى 10000 سيارة
- **التحميل:** قد يتباطأ مع أكثر من 100 صورة

### التوصيات
- احذف الصور غير الضرورية
- ضغط الصور قبل الرفع
- صدّر نسخة احتياطية بانتظام
- احذف السيارات القديمة

---

## 🚀 التطويرات المستقبلية

### قريباً
- [ ] دعم قاعدة بيانات خارجية (MySQL, MongoDB)
- [ ] ضغط الصور تلقائياً
- [ ] تصدير إلى Excel/PDF
- [ ] تقارير متقدمة
- [ ] إشعارات وتنبيهات

### مقترحات
- [ ] ربط مع نظام المخالفات
- [ ] تتبع الصيانة والتأمين
- [ ] خرائط مواقف السيارات
- [ ] تطبيق موبايل
- [ ] API للتكامل الخارجي

---

## 📞 الدعم والمساعدة

### الموقع المباشر
https://hilarious-narwhal-1a5720.netlify.app/

### صفحة إدارة السيارات
https://hilarious-narwhal-1a5720.netlify.app/pages/vehicles_management.html

### GitHub Repository
https://github.com/Ali5829511/N-M

### الإبلاغ عن مشاكل
افتح Issue جديد على GitHub أو تواصل مع فريق التطوير.

---

## 📝 سجل التغييرات

### الإصدار 1.0.0 (2024-01-15)
- ✅ إطلاق النظام الأول
- ✅ قاعدة بيانات شاملة للسيارات
- ✅ نظام تخزين الصور
- ✅ سجل التاريخ
- ✅ البحث والتصفية
- ✅ الإحصائيات
- ✅ الاستيراد والتصدير
- ✅ التكامل مع نظام التعرف على اللوحات
- ✅ التكامل مع نظام الملصقات
- ✅ واجهة مستخدم احترافية

---

## 📄 الترخيص

هذا النظام جزء من مشروع N-M لإدارة إسكان أعضاء هيئة التدريس.

جميع الحقوق محفوظة © 2024

---

## 🙏 شكر وتقدير

تم تطوير هذا النظام بواسطة فريق Manus AI.

شكراً لاستخدامك نظام قاعدة بيانات السيارات! 🚗✨
