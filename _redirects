# Netlify Redirects

# Redirect root to index
/               /index.html   200

# Allow pages directory
/pages/*        /pages/:splat 200

# SPA fallback for other routes
/*              /index.html   404
