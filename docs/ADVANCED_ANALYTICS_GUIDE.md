# 📊 دليل لوحة التحليلات المتقدمة
# Advanced Analytics Dashboard Guide

**الإصدار:** 1.2.1  
**تاريخ الإنشاء:** 2025-11-12  
**الحالة:** ✅ جاهز للاستخدام

---

## 📋 نظرة عامة - Overview

لوحة التحليلات المتقدمة هي صفحة شاملة لتحليل بيانات السيارات والمخالفات المرورية مع تتبع المخالفين المتكررين وإدارة قاعدة بيانات السيارات.

The Advanced Analytics Dashboard is a comprehensive page for analyzing vehicle and traffic violation data with repeat offender tracking and vehicle database management.

---

## ✨ الميزات الرئيسية - Key Features

### 1. 📊 الإحصائيات الفورية - Real-time Statistics

**بطاقات إحصائية تفاعلية:**
- ✅ **إجمالي السيارات المسجلة** - عدد السيارات في قاعدة البيانات
- ✅ **إجمالي المخالفات** - العدد الكلي للمخالفات المسجلة
- ✅ **المخالفون المتكررون** - عدد السيارات ذات المخالفات المتعددة (≥2)
- ✅ **المخالفات المحلولة** - عدد المخالفات التي تم حلها

### 2. 🏆 أكثر المخالفين تكراراً - Top Offenders

**جدول تفاعلي يعرض:**
- ترتيب المخالفين حسب عدد المخالفات
- رقم اللوحة لكل مخالف
- عدد المخالفات بشكل بارز
- حالة السيارة (نشط، تحذير، خطر)
- **وظيفة البحث:** بحث فوري عن مخالف معين

**تصنيف الحالات:**
- 🔵 **نشط** - أقل من 3 مخالفات
- 🟡 **تحذير** - من 3 إلى 4 مخالفات
- 🔴 **خطر** - 5 مخالفات أو أكثر

### 3. 🕐 أحدث المخالفات - Recent Violations

**جدول يعرض آخر 10 مخالفات مع:**
- التاريخ
- رقم اللوحة
- نوع المخالفة
- حالة المخالفة

**أدوات التصفية:**
- 🔍 **تصفية حسب النوع** - اختيار نوع معين من المخالفات
- 📅 **تصفية حسب التاريخ** - عرض مخالفات يوم محدد

### 4. 🗄️ قاعدة بيانات السيارات - Vehicles Database

**جدول شامل يعرض:**
- رقم اللوحة
- نوع السيارة
- اسم المالك
- عدد المخالفات
- تاريخ آخر مخالفة
- حالة السيارة

**وظيفة البحث المتقدم:**
- البحث برقم اللوحة
- البحث بنوع السيارة
- البحث باسم المالك

---

## 🗃️ بنية قاعدة البيانات - Database Structure

### قاعدة بيانات السيارات - Vehicles Database

**مخزنة في:** `localStorage.vehiclesDatabase`

**البنية:**
```javascript
{
    plateNumber: "ر ق ل 1234",        // رقم اللوحة (مفتاح رئيسي)
    vehicleType: "سيدان",              // نوع السيارة
    ownerName: "د. أحمد محمد",         // اسم المالك
    ownerPhone: "0501234567",          // رقم الهاتف
    ownerEmail: "ahmed@example.com",   // البريد الإلكتروني
    violationsCount: 5,                // عدد المخالفات
    lastViolationDate: "2025-11-10",  // تاريخ آخر مخالفة
    status: "خطر",                     // الحالة (نشط، تحذير، خطر)
    createdDate: "2025-11-01",        // تاريخ الإنشاء
    notes: "ملاحظات إضافية"           // ملاحظات
}
```

### قاعدة بيانات المخالفات - Violations Database

**مخزنة في:** `localStorage.violations`

**البنية:**
```javascript
{
    id: 1,                            // المعرف الفريد
    plateNumber: "ر ق ل 1234",       // رقم اللوحة
    vehicleType: "سيدان",             // نوع السيارة
    violationType: "توقف خاطئ",       // نوع المخالفة
    date: "2025-11-10",              // التاريخ
    time: "14:30",                   // الوقت
    location: "موقف المبنى 5",        // الموقع
    driverName: "د. أحمد محمد",       // اسم السائق
    status: "جديد",                   // الحالة
    notes: "ملاحظات"                  // ملاحظات
}
```

---

## 🔄 آلية المزامنة التلقائية - Auto-Sync Mechanism

### كيف تعمل المزامنة؟

1. **عند تحميل الصفحة:**
   - تحميل قاعدة بيانات السيارات من localStorage
   - تحميل قاعدة بيانات المخالفات من localStorage

2. **المزامنة التلقائية:**
   - البحث عن سيارات جديدة في المخالفات
   - إضافة السيارات غير الموجودة تلقائياً
   - حساب عدد المخالفات لكل سيارة
   - تحديث تاريخ آخر مخالفة
   - تحديث حالة السيارة بناءً على عدد المخالفات

3. **التحديث الدوري:**
   - تحديث البيانات كل 30 ثانية تلقائياً
   - مزامنة مع التغييرات في أي صفحة أخرى

### وظائف المزامنة في database.js

```javascript
// مزامنة السيارات من المخالفات
await db.syncVehiclesFromViolations();

// حساب عدد المخالفات
await db.calculateVehicleViolations();

// الحصول على المخالفين المتكررين
const offenders = await db.getRepeatedOffenders(2); // 2 مخالفات أو أكثر

// الحصول على إحصائيات متقدمة
const stats = await db.getAdvancedStatistics();
```

---

## 🎯 حالات السيارة - Vehicle Status

### منطق تحديد الحالة:

| عدد المخالفات | الحالة | اللون | الوصف |
|---------------|--------|-------|--------|
| 0 - 2 | **نشط** | 🟢 أخضر | لا توجد مشاكل |
| 3 - 4 | **تحذير** | 🟡 أصفر | يحتاج انتباه |
| 5+ | **خطر** | 🔴 أحمر | يحتاج إجراء فوري |

### التحديث التلقائي:
- يتم تحديث الحالة تلقائياً عند إضافة مخالفة جديدة
- يتم إعادة حساب الحالات عند تحميل الصفحة
- المزامنة كل 30 ثانية للحفاظ على دقة البيانات

---

## 📱 الاستخدام - Usage

### الوصول للصفحة:

**من لوحة التحكم الرئيسية:**
1. افتح `unified_dashboard.html`
2. اضغط على زر **"التحليلات المتقدمة"** في شريط التبويبات

**الرابط المباشر:**
```
pages/advanced_analytics_dashboard.html
```

### البحث والتصفية:

**البحث عن مخالف:**
1. في قسم "أكثر المخالفين تكراراً"
2. أدخل رقم اللوحة في مربع البحث
3. ستظهر النتائج فوراً

**البحث في قاعدة البيانات:**
1. في قسم "قاعدة بيانات السيارات"
2. أدخل رقم اللوحة أو نوع السيارة أو اسم المالك
3. البحث يتم في جميع الحقول

**تصفية المخالفات:**
1. في قسم "أحدث المخالفات"
2. اختر نوع المخالفة من القائمة المنسدلة
3. أو اختر تاريخ محدد
4. يمكن الجمع بين التصفيتين

---

## 🔧 وظائف JavaScript المتاحة - Available Functions

### في database.js:

```javascript
// الحصول على جميع السيارات
const vehicles = await db.getVehiclesDatabase();

// إضافة أو تحديث سيارة
await db.addOrUpdateVehicle({
    plateNumber: "ر ق ل 1234",
    vehicleType: "سيدان",
    ownerName: "د. أحمد محمد",
    ownerPhone: "0501234567"
});

// البحث في السيارات
const results = await db.searchVehicles("ر ق ل");

// الحصول على المخالفين المتكررين
const offenders = await db.getRepeatedOffenders(3); // 3 مخالفات أو أكثر

// حذف سيارة
await db.deleteVehicle("ر ق ل 1234");

// الحصول على إحصائيات
const stats = await db.getAdvancedStatistics();
// يعيد:
// - totalVehicles: إجمالي السيارات
// - totalViolations: إجمالي المخالفات
// - repeatedOffenders: المخالفون المتكررون
// - dangerousVehicles: السيارات في حالة خطر
// - warningVehicles: السيارات في حالة تحذير
// - violationsByType: المخالفات حسب النوع
// - violationsByMonth: المخالفات حسب الشهر
// - averageViolationsPerVehicle: متوسط المخالفات لكل سيارة
```

---

## 🎨 التصميم والواجهة - Design & UI

### الألوان المستخدمة:

- **Primary:** `#8B6F47` (البني الذهبي)
- **Secondary:** `#003c50` (الأزرق الداكن)
- **Success:** `#51cf66` (الأخضر)
- **Warning:** `#ffa94d` (البرتقالي)
- **Danger:** `#ff6b6b` (الأحمر)
- **Info:** `#339af0` (الأزرق)

### الخطوط:
- **الخط الرئيسي:** Cairo (من Google Fonts)
- **الوزن:** 400 (عادي)، 600 (متوسط)، 700 (عريض)

### التجاوب:
- ✅ متجاوب بالكامل مع جميع أحجام الشاشات
- ✅ تصميم Grid يتكيف تلقائياً
- ✅ جداول قابلة للتمرير على الشاشات الصغيرة

---

## 🔐 الصلاحيات - Permissions

### من يمكنه الوصول؟

**المطلوب:**
- ✅ تسجيل دخول نشط
- ✅ أي دور من أدوار النظام

**الأدوار المتاحة:**
1. **admin** - الوصول الكامل (قراءة، تعديل، حذف)
2. **violation_entry** - القراءة فقط
3. **inquiry** - القراءة فقط

**التحقق التلقائي:**
```javascript
if (!auth.currentUser) {
    window.location.href = '../index.html';
}
```

---

## 📊 أمثلة الاستخدام - Usage Examples

### مثال 1: عرض أكثر المخالفين

```javascript
// في الصفحة
function loadTopOffenders() {
    const topOffenders = vehiclesDatabase
        .filter(v => v.violationsCount > 0)
        .sort((a, b) => b.violationsCount - a.violationsCount)
        .slice(0, 10);
    
    // عرض في الجدول
    displayOffenders(topOffenders);
}
```

### مثال 2: تصفية المخالفات حسب النوع

```javascript
function filterViolations() {
    const filterType = document.getElementById('filterType').value;
    let filtered = violationsDatabase;
    
    if (filterType) {
        filtered = filtered.filter(v => v.violationType === filterType);
    }
    
    displayViolations(filtered);
}
```

### مثال 3: البحث في السيارات

```javascript
async function searchVehicles(searchTerm) {
    const results = await db.searchVehicles(searchTerm);
    displayVehicles(results);
}
```

---

## 🚀 التحسينات المستقبلية - Future Enhancements

### مخطط التطوير:

- [ ] **رسوم بيانية تفاعلية** - Chart.js أو D3.js
- [ ] **تصدير البيانات** - Excel, PDF, CSV
- [ ] **إشعارات فورية** - عند تجاوز حد المخالفات
- [ ] **تقارير مجدولة** - إرسال تلقائي عبر البريد
- [ ] **تحليلات متقدمة** - AI/ML للتنبؤ بالمخالفات
- [ ] **خرائط تفاعلية** - عرض مواقع المخالفات
- [ ] **مقارنة زمنية** - مقارنة الأشهر والسنوات
- [ ] **تقارير مخصصة** - إنشاء تقارير حسب الطلب

---

## 🐛 استكشاف الأخطاء - Troubleshooting

### المشكلة: البيانات لا تظهر

**الحلول:**
1. تحقق من وجود بيانات في localStorage
2. افتح Console واضغط: `localStorage.getItem('violations')`
3. تأكد من تسجيل الدخول

### المشكلة: العداد لا يتحدث

**الحلول:**
1. تحديث الصفحة (F5)
2. التحقق من JavaScript console للأخطاء
3. مسح cache المتصفح

### المشكلة: البحث لا يعمل

**الحلول:**
1. تأكد من إدخال رقم اللوحة صحيح
2. جرب البحث بجزء من النص
3. تحقق من حساسية الحروف (البحث غير حساس)

---

## 📞 الدعم الفني - Technical Support

### للمساعدة:
- 📧 **البريد الإلكتروني:** support@university.edu.sa
- 📱 **الهاتف:** +966 XX XXX XXXX
- 🌐 **الموقع:** https://ali5829511.github.io/N-M/

### التوثيق الإضافي:
- [دليل المستخدم الكامل](../README.md)
- [دليل قاعدة البيانات](DATABASE_INFO.md)
- [دليل الأمان](SECURITY.md)

---

## ✅ قائمة التحقق - Checklist

قبل استخدام الصفحة، تأكد من:

- [x] تسجيل الدخول للنظام
- [x] وجود بيانات مخالفات في النظام
- [x] المتصفح يدعم localStorage
- [x] JavaScript مفعل في المتصفح
- [x] الاتصال بالإنترنت (للخطوط والأيقونات)

---

**آخر تحديث:** 2025-11-12  
**الإصدار:** 1.2.1  
**الحالة:** ✅ جاهز للإنتاج

---

© 2025 - نظام إدارة المرور  
جامعة الإمام محمد بن سعود الإسلامية
