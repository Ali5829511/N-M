# 🆘 حل مشاكل الاتصال - Connection Troubleshooting

## المشكلة: لا يمكن الاتصال بـ localhost:8080

### السبب الأكثر شيوعاً
**الخادم غير مشغّل!** 🔴

يجب تشغيل الخادم أولاً قبل فتح المتصفح.

---

## 🔧 الحل الكامل خطوة بخطوة

### الخطوة 1: افتح نافذة Terminal/CMD

**Windows:**
- اضغط `Win + R`
- اكتب `cmd` واضغط Enter
- أو ابحث عن "Command Prompt" في قائمة Start

**Mac:**
- اضغط `Cmd + Space`
- اكتب `terminal` واضغط Enter

**Linux:**
- اضغط `Ctrl + Alt + T`

---

### الخطوة 2: انتقل لمجلد المشروع

```bash
cd /path/to/N-M
```

**مثال Windows:**
```cmd
cd C:\Users\YourName\Desktop\N-M
```

**مثال Mac/Linux:**
```bash
cd ~/Desktop/N-M
```

**💡 نصيحة:** يمكنك سحب المجلد وإفلاته في Terminal لنسخ المسار تلقائياً.

---

### الخطوة 3: فحص الإعداد (موصى به)

شغّل أداة الفحص التلقائي:

```bash
npm run check
```

ستخبرك هذه الأداة بأي مشاكل موجودة وكيفية إصلاحها.

---

### الخطوة 4: تثبيت المتطلبات

```bash
npm install
```

**انتظر حتى ينتهي التثبيت!** قد يستغرق 1-2 دقيقة.

**يجب أن تشاهد:**
```
added 132 packages
```

**إذا رأيت أخطاء:**
- تأكد من تثبيت Node.js: https://nodejs.org
- شغّل `npm install` مرة أخرى

---

### الخطوة 5: تشغيل الخادم

```bash
npm start
```

**⚠️ مهم جداً:** اترك هذه النافذة مفتوحة! لا تغلقها!

**يجب أن تشاهد:**
```
============================================================
🚀 نظام إدارة المرور - خادم محلي عالي الجودة
🚀 Traffic Management System - High-Quality Server
============================================================

✅ الخادم يعمل الآن / Server is running!

📡 العنوان المحلي / Local Address:
   http://localhost:8080
   http://127.0.0.1:8080
```

---

### الخطوة 6: افتح المتصفح

**الآن فقط** افتح Firefox أو أي متصفح آخر واذهب إلى:

```
http://localhost:8080
```

**يجب أن تشاهد صفحة تسجيل الدخول! ✅**

---

## 🔍 إذا لم يعمل بعد

### مشكلة 1: "npm: command not found"

**الحل:** ثبّت Node.js أولاً
1. اذهب إلى: https://nodejs.org
2. حمّل النسخة LTS (الموصى بها)
3. ثبّتها
4. أعد فتح Terminal/CMD
5. جرب `npm install` مرة أخرى

---

### مشكلة 2: "Cannot find module 'express'"

**الحل:** المتطلبات غير مثبتة بشكل صحيح

```bash
rm -rf node_modules
npm install
npm start
```

---

### مشكلة 3: "Port 8080 is already in use"

**الحل 1:** أوقف البرنامج الآخر الذي يستخدم المنفذ 8080

**الحل 2:** استخدم منفذ مختلف:
```bash
PORT=3000 npm start
```
ثم افتح: `http://localhost:3000`

---

### مشكلة 4: الخادم يعمل لكن الصفحة فارغة

**الحل:** تأكد من وجود ملف `index.html` في المجلد

```bash
ls index.html
```

إذا كان غير موجود، تأكد من أنك في المجلد الصحيح.

---

## 🐍 خيار بديل: استخدام Python

إذا لم يعمل Node.js، استخدم Python:

```bash
python3 simple-server.py
```

أو:

```bash
python3 -m http.server 8080
```

ثم افتح: `http://localhost:8080`

---

## 📝 قائمة التحقق السريعة

قبل فتح المتصفح، تأكد من:

- [ ] أنت في مجلد المشروع الصحيح (`cd /path/to/N-M`)
- [ ] شغّلت `npm install` ورأيت "added 132 packages"
- [ ] شغّلت `npm start` ورأيت "Server is running"
- [ ] نافذة Terminal/CMD لا تزال مفتوحة
- [ ] لم تضغط `Ctrl+C` في Terminal

---

## 🎥 فيديو توضيحي

الخطوات بالتفصيل:

1. **افتح Terminal** ✅
2. **اذهب للمجلد:** `cd /path/to/N-M` ✅
3. **ثبّت:** `npm install` ✅
4. **شغّل:** `npm start` ✅
5. **افتح المتصفح:** `http://localhost:8080` ✅

---

## 💡 نصائح مهمة

1. **لا تغلق Terminal** أثناء استخدام النظام
2. **لإيقاف الخادم:** اضغط `Ctrl+C` في نافذة Terminal
3. **لإعادة التشغيل:** شغّل `npm start` مرة أخرى

---

## 📞 لا يزال لا يعمل؟

شغّل أداة التشخيص الكاملة:

```bash
npm run check
```

ستخبرك بالضبط ما هي المشكلة وكيفية إصلاحها.

---

## 🆘 الحصول على المساعدة

إذا لم تنجح أي من الحلول أعلاه:

1. شغّل: `npm run check`
2. التقط صورة شاشة (screenshot) للنتيجة
3. التقط صورة شاشة للخطأ في Terminal
4. التقط صورة شاشة للخطأ في المتصفح
5. أرفق الصور في تعليق على GitHub

---

**تم إعداده بواسطة:** نظام إدارة المرور  
**للمزيد من المساعدة:** راجع `SERVER_SETUP_AR.md`

© 2025 - جامعة الإمام محمد بن سعود الإسلامية
