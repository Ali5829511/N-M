# تفعيل GitHub Pages - Enabling GitHub Pages
# دليل النشر للنظام

## 📋 نظرة عامة

هذا الدليل يوضح خطوات تفعيل وتكوين GitHub Pages لنشر النظام على الإنترنت.

## ✅ المتطلبات

- حساب GitHub
- مستودع (Repository) يحتوي على النظام
- صلاحيات المدير (Admin) على المستودع

## 🚀 خطوات التفعيل

### الطريقة 1: عبر واجهة GitHub (موصى بها)

#### الخطوة 1: الوصول إلى إعدادات المستودع
1. اذهب إلى مستودع النظام على GitHub
2. انقر على **Settings** (الإعدادات)

#### الخطوة 2: تفعيل GitHub Pages
1. في القائمة الجانبية، انقر على **Pages**
2. في قسم **Source** (المصدر):
   - اختر **GitHub Actions** من القائمة المنسدلة
   - سيتم تفعيل النشر التلقائي عبر GitHub Actions

#### الخطوة 3: التحقق من التفعيل
1. بعد التفعيل، سيظهر رابط النظام في أعلى الصفحة
2. الرابط سيكون بالشكل: `https://[username].github.io/[repository-name]/`
3. مثال: `https://ali5829511.github.io/N-M/`

#### الخطوة 4: تشغيل Workflow
1. اذهب إلى تبويب **Actions**
2. ستجد workflow باسم "Deploy to GitHub Pages"
3. انقر على **Run workflow** > **Run workflow** لتشغيله يدوياً
4. انتظر 1-2 دقيقة حتى يكتمل النشر
5. افتح الرابط للتحقق من النظام

### الطريقة 2: عبر ملف الإعدادات (متقدم)

إذا كنت تفضل التكوين عبر الأوامر:

```bash
# استخدام GitHub CLI
gh api repos/Ali5829511/N-M/pages \
  --method POST \
  -f source[branch]=main \
  -f build_type=workflow
```

## 🔍 التحقق من حالة النشر

### عبر واجهة GitHub:
1. اذهب إلى **Actions**
2. ستجد قائمة بجميع عمليات النشر
3. ابحث عن "Deploy to GitHub Pages"
4. الرمز الأخضر (✓) يعني نجاح النشر
5. الرمز الأحمر (✗) يعني فشل النشر

### عبر الرابط المباشر:
- افتح الرابط: `https://[username].github.io/[repository-name]/`
- إذا ظهر النظام، النشر ناجح ✓
- إذا ظهرت صفحة 404، تحقق من الإعدادات

## ⚙️ التكوينات المتقدمة

### 1. استخدام نطاق مخصص (Custom Domain)

إذا كنت تملك نطاقاً خاصاً:

#### الخطوة 1: إضافة ملف CNAME
```bash
echo "yourdomain.com" > CNAME
git add CNAME
git commit -m "Add custom domain"
git push
```

#### الخطوة 2: تكوين DNS
في إعدادات النطاق، أضف:
```
Type: CNAME
Name: www
Value: [username].github.io
```

أو للنطاق الرئيسي:
```
Type: A
Name: @
Value: 185.199.108.153
Value: 185.199.109.153
Value: 185.199.110.153
Value: 185.199.111.153
```

#### الخطوة 3: تكوين GitHub Pages
1. اذهب إلى **Settings** > **Pages**
2. في قسم **Custom domain**، أدخل `yourdomain.com`
3. فعّل **Enforce HTTPS**

### 2. تحسين الأداء

#### تفعيل الضغط:
الملف `.github/workflows/deploy.yml` يتضمن بالفعل الإعدادات المثلى.

#### تحسين الصور:
```bash
# تصغير حجم الصور قبل النشر
for img in *.jpg *.png; do
    convert "$img" -quality 85 "$img"
done
```

### 3. تكوين HTTPS

GitHub Pages يوفر HTTPS تلقائياً ✓

للتأكد:
1. اذهب إلى **Settings** > **Pages**
2. فعّل **Enforce HTTPS**
3. جميع الاتصالات ستكون مشفرة

## 🐛 استكشاف الأخطاء

### المشكلة 1: صفحة 404
**السبب:** GitHub Pages غير مفعّل أو التكوين خاطئ

**الحل:**
1. تأكد من تفعيل Pages في Settings
2. تأكد من اختيار **GitHub Actions** كمصدر
3. تأكد من وجود ملف `index.html` في المجلد الرئيسي

### المشكلة 2: فشل Workflow
**السبب:** خطأ في التكوين أو الصلاحيات

**الحل:**
1. اذهب إلى **Settings** > **Actions** > **General**
2. في **Workflow permissions**، اختر:
   - ✓ Read and write permissions
3. في **Actions permissions**، تأكد من:
   - ✓ Allow all actions and reusable workflows

### المشكلة 3: النظام لا يعمل بعد النشر
**السبب:** مشكلة في المسارات أو الملفات

**الحل:**
1. افتح Developer Console (F12)
2. تحقق من الأخطاء في Console
3. تأكد من تحميل جميع ملفات CSS و JavaScript
4. تحقق من المسارات النسبية للملفات

### المشكلة 4: لا يمكن الوصول للنظام
**السبب:** المستودع خاص (Private)

**الحل:**
- GitHub Pages مجاني للمستودعات العامة (Public)
- للمستودعات الخاصة، تحتاج حساب GitHub Pro أو Enterprise

## 📊 مراقبة النشر

### بعد كل تحديث:
1. يتم تشغيل Workflow تلقائياً
2. يستغرق النشر 1-2 دقيقة
3. يمكنك متابعة التقدم في تبويب **Actions**

### الإشعارات:
- GitHub يرسل إشعارات عند فشل النشر
- يمكنك إعداد Webhooks للإشعارات المخصصة

## 🔐 الأمان

### ملاحظات مهمة:
1. **لا تنشر معلومات حساسة** في المستودع العام
2. **استخدم Environment Variables** للمفاتيح السرية
3. **فعّل HTTPS** دائماً
4. **راجع SECURITY.md** للمزيد من الإرشادات

### للبيانات الحساسة:
- استخدم Backend API منفصل
- لا تخزن كلمات المرور في localStorage
- استخدم JWT tokens للمصادقة

## 📝 قائمة التحقق

قبل نشر النظام، تأكد من:

- [ ] تم تفعيل GitHub Pages
- [ ] تم اختيار GitHub Actions كمصدر
- [ ] تم تشغيل Workflow بنجاح
- [ ] النظام يعمل على الرابط المنشور
- [ ] جميع الصفحات تعمل بشكل صحيح
- [ ] تم اختبار تسجيل الدخول
- [ ] تم اختبار جميع الوظائف
- [ ] تم تفعيل HTTPS
- [ ] تم مراجعة الأمان
- [ ] تم توثيق الرابط في README

## 🌟 نصائح للنجاح

1. **اختبر محلياً أولاً** قبل النشر
2. **استخدم Git بشكل صحيح** - commits واضحة
3. **راقب Workflow** للتأكد من النجاح
4. **احتفظ بنسخة احتياطية** من البيانات
5. **وثّق التغييرات** في CHANGELOG

## 📞 الدعم

إذا واجهت مشاكل:

1. **راجع الأخطاء في Actions**
   - اذهب إلى Actions > اختر الـ workflow الفاشل
   - اقرأ رسائل الخطأ بعناية

2. **راجع التوثيق:**
   - [GitHub Pages Documentation](https://docs.github.com/en/pages)
   - [GitHub Actions Documentation](https://docs.github.com/en/actions)

3. **راجع ملفات المشروع:**
   - README.md
   - DEPLOYMENT.md
   - SECURITY.md

## 🎯 الحالة الحالية

### للمستودع Ali5829511/N-M:

**الحالة:** GitHub Pages جاهز للتفعيل ⏳

**الخطوة التالية:**
1. اذهب إلى Settings > Pages
2. اختر GitHub Actions
3. شغّل الـ workflow
4. النظام سيكون متاحاً على:
   - `https://ali5829511.github.io/N-M/`

## 📅 بعد النشر

### الصيانة الدورية:
- تحديث النظام بانتظام
- مراقبة الأداء
- النسخ الاحتياطي للبيانات
- مراجعة الأمان

### التطوير المستقبلي:
- إضافة ميزات جديدة
- تحسين الأداء
- تحسين تجربة المستخدم
- الانتقال لقاعدة بيانات حقيقية

---

**آخر تحديث:** 2025-11-08  
**الحالة:** جاهز للتفعيل ✅  
**الإجراء المطلوب:** تفعيل GitHub Pages من Settings
