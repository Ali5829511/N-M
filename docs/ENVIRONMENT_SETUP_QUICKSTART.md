# ⚡ البدء السريع مع .env - Quick Start with .env

## 🎯 في 3 خطوات فقط - Just 3 Steps!

### الخطوة 1: نسخ الملف - Copy the File

**Windows:**
```bash
copy .env.example .env
```

**Linux/Mac:**
```bash
cp .env.example .env
```

### الخطوة 2: تعديل الإعدادات (اختياري) - Edit Settings (Optional)

افتح ملف `.env` بأي محرر نصوص وغيّر القيم حسب الحاجة:

```env
# الإعدادات الأساسية (تعمل بشكل افتراضي)
PORT=8080
HOST=0.0.0.0

# ParkPow API (اختياري - أضف التوكن إذا كان لديك حساب)
PARKPOW_API_TOKEN=your_token_here
```

⚠️ **ملاحظة:** النظام يعمل بدون تعديل أي شيء! التعديلات اختيارية للميزات المتقدمة فقط.

### الخطوة 3: تشغيل الخادم - Start Server

```bash
npm install
npm start
```

افتح المتصفح على: http://localhost:8080

---

## ✅ تم! - Done!

النظام الآن يعمل ويستخدم dotenv لتحميل الإعدادات من ملف `.env`

---

## 🔍 كيف أعرف أن dotenv يعمل؟ - How do I know dotenv works?

عند تشغيل `npm start`، ستشاهد:

```
[dotenv@17.2.3] injecting env (X) from .env
```

هذا يعني أن dotenv قام بتحميل المتغيرات من ملف `.env` بنجاح!

---

## 🚀 الخطوات التالية - Next Steps

إذا كنت تريد استخدام ميزات متقدمة:

### 1️⃣ تفعيل ParkPow API للتعرف على اللوحات

1. احصل على توكن من: https://app.parkpow.com
2. افتح `.env` وأضف:
   ```env
   PARKPOW_API_TOKEN=your_actual_token_here
   ```
3. أعد تشغيل الخادم

### 2️⃣ ربط قاعدة بيانات

1. احصل على رابط قاعدة البيانات من Neon.tech
2. افتح `.env` وأضف:
   ```env
   DATABASE_URL=postgresql://user:pass@host/db
   ```
3. أعد تشغيل الخادم

### 3️⃣ تفعيل البريد الإلكتروني

1. احصل على مفاتيح من EmailJS.com
2. افتح `.env` وأضف:
   ```env
   EMAILJS_SERVICE_ID=your_service_id
   EMAILJS_PUBLIC_KEY=your_public_key
   ```
3. أعد تشغيل الخادم

---

## 📚 المزيد من المعلومات - More Information

للحصول على دليل شامل ومفصل:
📖 **[ENVIRONMENT_SETUP.md](ENVIRONMENT_SETUP.md)** - الدليل الكامل

---

## 🆘 مشاكل؟ - Problems?

### المشكلة: "Cannot find module 'dotenv'"
**الحل:**
```bash
npm install
```

### المشكلة: التغييرات في .env لا تظهر
**الحل:** أعد تشغيل الخادم (Ctrl+C ثم npm start)

### المشكلة: "PARKPOW_API_TOKEN is not set"
**الحل:** هذا تحذير فقط. النظام يعمل! إذا كنت تريد استخدام ParkPow API، أضف التوكن في ملف `.env`

---

**💡 نصيحة:** لا تقلق! النظام يعمل بشكل ممتاز حتى بدون إضافة أي إعدادات في `.env` 😊
