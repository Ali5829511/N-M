# دليل نظام المصادقة - Authentication Guide

## 📚 نظرة عامة

النظام يستخدم نظام مصادقة آمن مع تشفير SHA-256 لكلمات المرور. هذا الدليل يشرح كيفية استخدام النظام في بيئات التطوير والإنتاج.

---

## 🔐 نظام التشفير

### التحسينات الأمنية الحالية:
- ✅ تشفير كلمات المرور باستخدام **SHA-256**
- ✅ توليد كلمات مرور عشوائية قوية (16 حرف)
- ✅ دعم كلمات المرور المؤقتة
- ✅ إجبار المستخدم على تغيير كلمة المرور عند أول تسجيل دخول
- ✅ نظام جلسات آمن (Session Management)
- ✅ مهلة تلقائية للجلسات (30 دقيقة)

---

## 🚀 البدء السريع

### 1. أول تشغيل للنظام

عند فتح النظام لأول مرة:

1. افتح صفحة تسجيل الدخول `index.html`
2. افتح **Console المتصفح** (اضغط F12، ثم اختر Console)
3. ستجد رسالة تحتوي على كلمات المرور المؤقتة:

```
🔐 تم إنشاء المستخدمين بكلمات مرور آمنة:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
👤 admin: Xy8k#mP2@nQ5$vL9
👤 violations_officer: Rt4w!bN7&cF3@hM6
👤 inquiry_user: Js9p#dK2@wV8$tL5
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
⚠️ احفظ هذه الكلمات في مكان آمن!
⚠️ يجب تغييرها عند أول تسجيل دخول.
```

4. **احفظ هذه الكلمات في مكان آمن!**
5. استخدم اسم المستخدم وكلمة المرور المؤقتة لتسجيل الدخول

---

## 🛠️ للمطورين - أدوات التطوير

### أداة إدارة كلمات المرور

لتسهيل التطوير والاختبار، يوفر النظام أداة مخصصة:

**الموقع**: `pages/dev_password_manager.html`

#### الميزات:
- ✅ عرض جميع المستخدمين الحاليين
- ✅ عرض كلمات المرور المؤقتة
- ✅ نسخ كلمات المرور بنقرة واحدة
- ✅ إعادة توليد كلمات مرور جديدة
- ✅ إعادة تعيين كلمات المرور إلى القيم الافتراضية البسيطة

#### الاستخدام:

```bash
# افتح الملف مباشرة في المتصفح
pages/dev_password_manager.html

# أو عبر الخادم المحلي
http://localhost:8080/pages/dev_password_manager.html
```

#### إعادة تعيين إلى القيم الافتراضية (للتطوير فقط):

إذا كنت تريد استخدام كلمات مرور بسيطة للتطوير:

1. افتح `pages/dev_password_manager.html`
2. اضغط على "إعادة تعيين كلمات المرور الافتراضية"
3. سيتم تعيين:
   - `admin` ← `admin123`
   - `violations_officer` ← `violations123`
   - `inquiry_user` ← `inquiry123`

⚠️ **تحذير**: لا تستخدم القيم الافتراضية البسيطة في بيئة الإنتاج!

---

## 👥 المستخدمون الافتراضيون

### 1. مدير النظام (Admin)
- **اسم المستخدم**: `admin`
- **البريد**: `aliayashi522@gmail.com`
- **الصلاحيات**: الوصول الكامل لجميع وظائف النظام

### 2. مسجل المخالفات (Violation Entry)
- **اسم المستخدم**: `violations_officer`
- **البريد**: `violations@university.edu.sa`
- **الصلاحيات**: إضافة وعرض المخالفات فقط

### 3. موظف الاستعلام (Inquiry)
- **اسم المستخدم**: `inquiry_user`
- **البريد**: `inquiry@university.edu.sa`
- **الصلاحيات**: البحث والاستعلام والتصدير فقط

---

## 🔄 استرجاع كلمة المرور

### الطريقة 1: عبر واجهة النظام

1. في صفحة تسجيل الدخول، اضغط على "نسيت كلمة المرور؟"
2. أدخل اسم المستخدم أو البريد الإلكتروني
3. سيتم إنشاء كلمة مرور جديدة وعرضها في Console المتصفح
4. استخدم كلمة المرور الجديدة لتسجيل الدخول

### الطريقة 2: استخدام أداة إدارة كلمات المرور

1. افتح `pages/dev_password_manager.html`
2. ستجد جميع كلمات المرور المؤقتة الحالية
3. يمكنك نسخها مباشرة أو إعادة توليدها

---

## 🔒 الأمان في بيئة الإنتاج

### التوصيات للإنتاج:

1. **قاعدة بيانات خلفية**:
   - استخدم PostgreSQL أو MongoDB أو MySQL
   - لا تستخدم localStorage في الإنتاج

2. **تشفير أقوى**:
   - استبدل SHA-256 بـ bcrypt أو argon2
   - SHA-256 جيد للتطوير، لكنه ليس الأفضل للإنتاج

3. **API خلفي آمن**:
   - بناء REST API مع Node.js/Express أو Python/Django
   - استخدام JWT Tokens بدلاً من localStorage

4. **HTTPS**:
   - تفعيل SSL/TLS إجباري
   - جميع الاتصالات يجب أن تكون مشفرة

5. **Rate Limiting**:
   - حماية من هجمات Brute Force
   - تحديد عدد محاولات تسجيل الدخول

6. **Two-Factor Authentication (2FA)**:
   - إضافة طبقة أمان إضافية
   - استخدام Google Authenticator أو SMS

7. **Audit Logs**:
   - تسجيل جميع عمليات تسجيل الدخول
   - تتبع الأنشطة المشبوهة

---

## 🐛 حل المشكلات الشائعة

### المشكلة 1: "اسم المستخدم أو كلمة المرور غير صحيحة"

**الحلول**:
1. تأكد من استخدام كلمة المرور المؤقتة من Console
2. افتح `pages/dev_password_manager.html` لعرض كلمات المرور
3. تأكد من تفعيل JavaScript في المتصفح
4. امسح localStorage وأعد تحميل الصفحة (سيتم إنشاء مستخدمين جدد)

```javascript
// في Console المتصفح، لمسح جميع البيانات:
localStorage.clear();
location.reload();
```

### المشكلة 2: "حدث خطأ أثناء معالجة طلبك"

**الحلول**:
1. تأكد من تحميل ملف `js/crypto-utils.js` بشكل صحيح
2. تحقق من Console المتصفح لمعرفة التفاصيل
3. تأكد من أن المتصفح يدعم Web Crypto API
4. حاول استخدام متصفح آخر (Chrome أو Firefox موصى بهما)

### المشكلة 3: فقدت كلمات المرور المؤقتة

**الحلول**:
1. افتح `pages/dev_password_manager.html`
2. اختر "إعادة توليد كلمات مرور جديدة"
3. أو اختر "إعادة تعيين كلمات المرور الافتراضية" (للتطوير فقط)

### المشكلة 4: الصفحة تطلب كلمة مرور جديدة عند كل تسجيل دخول

**السبب**: العلامة `requirePasswordChange` مفعلة

**الحل**:
```javascript
// في Console المتصفح:
const users = JSON.parse(localStorage.getItem('users'));
users.forEach(u => u.requirePasswordChange = false);
localStorage.setItem('users', JSON.stringify(users));
location.reload();
```

---

## 📖 الكود البرمجي

### تشفير كلمة المرور:

```javascript
// استخدام CryptoUtils لتشفير كلمة المرور
const password = 'mySecurePassword123';
const hashedPassword = await CryptoUtils.hashPassword(password);
console.log('كلمة المرور المشفرة:', hashedPassword);
```

### التحقق من كلمة المرور:

```javascript
// التحقق من كلمة المرور
const isValid = await CryptoUtils.verifyPassword(
    'mySecurePassword123', 
    hashedPassword
);
console.log('كلمة المرور صحيحة؟', isValid);
```

### توليد كلمة مرور قوية:

```javascript
// توليد كلمة مرور عشوائية قوية
const newPassword = CryptoUtils.generateSecurePassword(16);
console.log('كلمة مرور جديدة:', newPassword);
```

---

## 📞 الدعم

إذا واجهت مشكلة:
1. راجع قسم حل المشكلات أعلاه
2. تحقق من Console المتصفح للحصول على تفاصيل الخطأ
3. راجع [SECURITY.md](../SECURITY.md) لمزيد من المعلومات الأمنية
4. تواصل مع فريق التطوير

---

## 📝 الترخيص

جميع الحقوق محفوظة © 2025 - جامعة الإمام محمد بن سعود الإسلامية
