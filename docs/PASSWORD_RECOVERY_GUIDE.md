# 🔐 دليل استرجاع كلمة المرور
# Password Recovery Guide

---

## 📋 نظرة عامة - Overview

تم إضافة ميزة استرجاع كلمة المرور لتمكين المستخدمين من إعادة تعيين كلمات المرور المنسية بطريقة آمنة.

This feature enables users to securely reset forgotten passwords.

---

## 🚀 كيفية الاستخدام - How to Use

### للمستخدمين - For Users

#### الخطوة 1: الوصول إلى صفحة الاسترجاع
1. افتح صفحة تسجيل الدخول
2. اضغط على رابط "نسيت كلمة المرور؟"

#### الخطوة 2: إدخال المعلومات
- أدخل **اسم المستخدم** أو **البريد الإلكتروني**
- اضغط على زر "إرسال رابط الاسترجاع"

#### الخطوة 3: الحصول على كلمة المرور المؤقتة
- **إذا كان البريد الإلكتروني مفعل**: ستصلك كلمة المرور على بريدك الإلكتروني
- **إذا لم يكن مفعل**: افتح Console في المتصفح (F12) وانسخ كلمة المرور

#### الخطوة 4: تسجيل الدخول
1. ارجع إلى صفحة تسجيل الدخول
2. استخدم كلمة المرور المؤقتة
3. **مهم**: يجب تغيير كلمة المرور عند أول تسجيل دخول

---

## 🔒 الأمان - Security

### ميزات الأمان المطبقة - Security Features

#### ✅ كلمات مرور آمنة
- **الطول**: 16 حرف على الأقل
- **التعقيد**: أحرف كبيرة وصغيرة، أرقام، رموز خاصة
- **مثال**: `6Txn45$31dC5VApe`

#### ✅ التشفير
- جميع كلمات المرور مشفرة باستخدام **SHA-256**
- لا يتم تخزين كلمات المرور بشكل نصي

#### ✅ التحقق من الصلاحيات
- التحقق من وجود المستخدم
- التحقق من حالة الحساب (نشط/معطل)
- منع الوصول للحسابات المعطلة

#### ✅ إجبار تغيير كلمة المرور
- علامة `requirePasswordChange` يتم تفعيلها تلقائياً
- المستخدم يجب أن يغير كلمة المرور المؤقتة

---

## 📧 إعدادات البريد الإلكتروني - Email Settings

### تفعيل إرسال البريد الإلكتروني

#### الطريقة 1: من واجهة الإعدادات
1. افتح صفحة **إعدادات البريد الإلكتروني** (`pages/email_settings.html`)
2. أدخل بيانات EmailJS:
   - Service ID
   - Public Key
   - Template IDs
3. فعّل "إشعارات إعادة تعيين كلمة المرور"
4. احفظ الإعدادات

#### الطريقة 2: EmailJS Setup
1. أنشئ حساب على [EmailJS](https://www.emailjs.com/)
2. أنشئ قالب بريد إلكتروني بالمتغيرات التالية:
   - `{{to_email}}` - بريد المستلم
   - `{{username}}` - اسم المستخدم
   - `{{temp_password}}` - كلمة المرور المؤقتة
   - `{{login_url}}` - رابط تسجيل الدخول
   - `{{system_name}}` - اسم النظام
   - `{{reset_date}}` - تاريخ إعادة التعيين

3. احصل على:
   - Service ID من لوحة التحكم
   - Public Key من Account Settings
   - Template ID من Email Templates

---

## 🛠️ للمطورين - For Developers

### API Reference

#### `authManager.requestPasswordReset(identifier)`

**الوصف**: طلب إعادة تعيين كلمة المرور

**المعاملات**:
- `identifier` (string): اسم المستخدم أو البريد الإلكتروني

**القيمة المرجعة**:
```javascript
{
    success: boolean,
    message?: string,
    error?: string
}
```

**مثال**:
```javascript
const result = await window.authManager.requestPasswordReset('admin');
if (result.success) {
    console.log(result.message);
} else {
    console.error(result.error);
}
```

#### `emailService.sendPasswordResetEmail(email, username, tempPassword)`

**الوصف**: إرسال بريد إلكتروني بكلمة المرور المؤقتة

**المعاملات**:
- `email` (string): البريد الإلكتروني للمستخدم
- `username` (string): اسم المستخدم
- `tempPassword` (string): كلمة المرور المؤقتة

**القيمة المرجعة**:
```javascript
{
    success: boolean,
    message?: string,
    error?: string
}
```

---

## 🔍 استكشاف الأخطاء - Troubleshooting

### المشكلة: لا تظهر رسالة النجاح

**الحل**:
1. تحقق من أن اسم المستخدم أو البريد الإلكتروني صحيح
2. تحقق من حالة الحساب (يجب أن يكون نشطاً)
3. افتح Console للاطلاع على الأخطاء

### المشكلة: لم يصل البريد الإلكتروني

**الحل**:
1. تحقق من تفعيل خدمة البريد الإلكتروني
2. تحقق من صحة إعدادات EmailJS
3. تحقق من مجلد الرسائل غير المرغوب فيها (Spam)
4. استخدم كلمة المرور من Console كبديل

### المشكلة: كلمة المرور المؤقتة لا تعمل

**الحل**:
1. انسخ كلمة المرور كاملة من Console (بدون مسافات)
2. تأكد من عدم وجود مسافات قبل أو بعد النص
3. جرب نسخ النص مباشرة من Console بدلاً من كتابته يدوياً

---

## 📊 الإحصائيات - Statistics

### معلومات الميزة - Feature Info

- **تاريخ الإضافة**: 2025-01-15
- **الإصدار**: 1.0.0
- **الحالة**: ✅ نشط ويعمل بكامل الوظائف

### التوافق - Compatibility

- ✅ جميع المتصفحات الحديثة
- ✅ الأجهزة المحمولة والحواسيب
- ✅ يعمل مع أو بدون خدمة البريد الإلكتروني

---

## ⚠️ ملاحظات مهمة - Important Notes

### للمسؤولين - For Administrators

1. **كلمات المرور المؤقتة تظهر في Console**
   - هذا للتأكد من إمكانية الوصول حتى بدون بريد إلكتروني
   - يجب على المسؤول مراقبة الوصول إلى Console في بيئة الإنتاج

2. **تفعيل البريد الإلكتروني موصى به**
   - لتحسين تجربة المستخدم
   - لزيادة الأمان

3. **النسخ الاحتياطي**
   - احتفظ بنسخة احتياطية من بيانات المستخدمين
   - وثق كلمات المرور المؤقتة إذا لزم الأمر

### للمستخدمين - For Users

1. **غيّر كلمة المرور فوراً**
   - لا تستخدم كلمة المرور المؤقتة لفترة طويلة
   - اختر كلمة مرور قوية وفريدة

2. **احتفظ بكلمة المرور في مكان آمن**
   - استخدم مدير كلمات المرور
   - لا تشارك كلمة المرور مع أحد

3. **راجع نشاط حسابك**
   - تحقق من آخر تسجيل دخول
   - أبلغ عن أي نشاط مشبوه

---

## 🔄 الإصدارات القادمة - Future Enhancements

### مخطط له - Planned

- [ ] إضافة رابط انتهاء صلاحية لطلبات إعادة التعيين
- [ ] إضافة سجل لطلبات إعادة تعيين كلمة المرور
- [ ] إضافة رمز OTP بدلاً من كلمة مرور مؤقتة
- [ ] إضافة أسئلة أمان إضافية
- [ ] إضافة مصادقة ثنائية (2FA)

---

## 📞 الدعم الفني - Technical Support

للمزيد من المعلومات أو الإبلاغ عن مشاكل:

- 📧 البريد الإلكتروني: support@university.edu.sa
- 📖 التوثيق الكامل: [README.md](../README.md)
- 🔒 الأمان: [SECURITY.md](../SECURITY.md)

---

## 📄 الترخيص - License

جميع الحقوق محفوظة © 2025 - جامعة الإمام محمد بن سعود الإسلامية
