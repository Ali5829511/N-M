# 🚀 دليل إعداد وتشغيل الخادم المحلي
# Local Server Setup Guide

## 📋 نظرة عامة

تم تجهيز نظام إدارة المرور بثلاثة خيارات عالية الجودة لتشغيل خادم محلي:

### الخيارات المتاحة:

1. **Express.js Server** (موصى به ⭐)
   - خادم احترافي عالي الأداء
   - ضغط تلقائي للملفات
   - معالجة متقدمة للأخطاء
   - دعم كامل لـ CORS
   - سجلات مفصلة

2. **HTTP-Server** (سريع وبسيط)
   - خادم Node.js خفيف الوزن
   - فتح تلقائي في المتصفح
   - تحديث سريع

3. **Python HTTP Server** (تقليدي)
   - لا يحتاج تثبيت إضافي
   - بسيط ومباشر

---

## 🎯 الخيار الأول: Express.js Server (موصى به)

### المميزات:
- ✅ أداء عالي مع ضغط الملفات
- ✅ معالجة احترافية للأخطاء
- ✅ سجلات مفصلة لجميع الطلبات
- ✅ دعم كامل لـ CORS
- ✅ إعدادات Cache ذكية
- ✅ صفحات خطأ مخصصة بالعربية
- ✅ عرض عناوين IP للشبكة المحلية

### التثبيت:

```bash
# تثبيت المتطلبات
npm install
```

### التشغيل:

```bash
# التشغيل العادي
npm start

# أو بشكل مباشر
npm run start:express

# التشغيل مع التحديث التلقائي (للتطوير)
npm run dev
```

### الوصول للنظام:

بعد تشغيل الخادم، سيظهر لك:
- **العنوان المحلي**: `http://localhost:8080`
- **عنوان IP**: `http://192.168.x.x:8080` (للوصول من أجهزة أخرى في الشبكة)

### إيقاف الخادم:

اضغط `Ctrl + C` في نافذة الأوامر

---

## ⚡ الخيار الثاني: HTTP-Server

### المميزات:
- ✅ خفيف وسريع جداً
- ✅ يفتح المتصفح تلقائياً
- ✅ بسيط في الاستخدام
- ✅ دعم CORS

### التثبيت:

```bash
npm install
```

### التشغيل:

```bash
npm run start:http-server
```

سيفتح المتصفح تلقائياً على `http://localhost:8080`

---

## 🐍 الخيار الثالث: Python HTTP Server

### المميزات:
- ✅ لا يحتاج تثبيت (Python مثبت مسبقاً)
- ✅ بسيط جداً
- ✅ مناسب للاختبار السريع

### التشغيل:

```bash
npm run start:python
```

أو مباشرة:

```bash
python3 -m http.server 8080
```

افتح المتصفح يدوياً على `http://localhost:8080`

---

## 🔧 إعدادات متقدمة

### تغيير رقم المنفذ (Port):

#### Express Server:
```bash
PORT=3000 npm start
```

#### HTTP-Server:
عدّل في `package.json` رقم المنفذ `-p 8080` إلى الرقم المطلوب

#### Python Server:
```bash
python3 -m http.server 3000
```

### تغيير العنوان المضيف (Host):

```bash
HOST=127.0.0.1 PORT=8080 npm start
```

---

## 📊 مقارنة الخيارات

| الميزة | Express.js | HTTP-Server | Python |
|--------|-----------|-------------|---------|
| **الأداء** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ |
| **السهولة** | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **الميزات** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐ |
| **الاحترافية** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ |
| **التثبيت** | يحتاج npm | يحتاج npm | لا يحتاج |
| **معالجة الأخطاء** | ممتازة | جيدة | أساسية |
| **CORS** | نعم | نعم | لا |
| **ضغط الملفات** | نعم | لا | لا |
| **السجلات** | مفصلة | أساسية | أساسية |

---

## 🎓 الاستخدام الموصى به

### للتطوير والاختبار:
- استخدم **Express.js** مع `npm run dev` للتحديث التلقائي

### للعرض التجريبي:
- استخدم **Express.js** مع `npm start` لتجربة احترافية

### للاختبار السريع:
- استخدم **HTTP-Server** مع `npm run start:http-server`

### بدون Node.js:
- استخدم **Python Server** مع `npm run start:python`

---

## 🔐 الأمان

⚠️ **تحذير مهم:**

هذه الخوادم مصممة **للتطوير والاختبار المحلي فقط**!

### للاستخدام في بيئة الإنتاج:

1. ✅ استخدم خادم ويب احترافي (Nginx, Apache)
2. ✅ فعّل HTTPS/SSL
3. ✅ قم بإعداد جدار ناري (Firewall)
4. ✅ استخدم نظام مصادقة قوي
5. ✅ راجع دليل [SECURITY.md](SECURITY.md)
6. ✅ راجع [DEPLOYMENT.md](DEPLOYMENT.md) للنشر الكامل

---

## 🌐 الوصول من الشبكة المحلية

### للسماح للأجهزة الأخرى بالوصول:

1. شغّل الخادم بأي من الطرق أعلاه
2. احصل على عنوان IP الخاص بك:

**Windows:**
```cmd
ipconfig
```

**Linux/Mac:**
```bash
ifconfig
# أو
ip addr show
```

3. على الجهاز الآخر، افتح المتصفح واذهب إلى:
   ```
   http://[عنوان-IP]:8080
   ```

مثال: `http://192.168.1.100:8080`

⚠️ **ملاحظة**: تأكد من:
- عدم وجود جدار ناري يمنع الاتصال
- وجود الأجهزة على نفس الشبكة

---

## 🆘 حل المشاكل الشائعة

### المشكلة: "المنفذ مستخدم بالفعل" (Port already in use)

**الحل:**
```bash
# جرب منفذ مختلف
PORT=3000 npm start
```

أو أوقف التطبيق الذي يستخدم المنفذ:

**Windows:**
```cmd
netstat -ano | findstr :8080
taskkill /PID [رقم_العملية] /F
```

**Linux/Mac:**
```bash
lsof -ti:8080 | xargs kill
```

### المشكلة: "npm: command not found"

**الحل:**
- ثبّت Node.js من [nodejs.org](https://nodejs.org)
- أو استخدم Python Server

### المشكلة: "Cannot GET /"

**الحل:**
- تأكد من وجود `index.html` في المجلد الحالي
- تأكد من تشغيل الأمر من مجلد المشروع الصحيح

### المشكلة: الصفحة لا تتحدث

**الحل:**
- استخدم `npm run dev` للتحديث التلقائي
- أو حدّث الصفحة يدوياً (F5 أو Ctrl+R)

### المشكلة: "EACCES: permission denied"

**الحل:**
```bash
# استخدم منفذ أكبر من 1024
PORT=8080 npm start

# أو على Linux/Mac
sudo npm start  # (غير موصى به)
```

---

## 📱 التوافق

### المتطلبات:

**للـ Express.js و HTTP-Server:**
- Node.js 14.0 أو أحدث
- npm 6.0 أو أحدث

**للـ Python Server:**
- Python 3.x

### المتصفحات المدعومة:
- ✅ Chrome 90+
- ✅ Firefox 88+
- ✅ Safari 14+
- ✅ Edge 90+
- ✅ Opera 76+

### أنظمة التشغيل:
- ✅ Windows 10/11
- ✅ macOS 10.15+
- ✅ Linux (Ubuntu, Debian, Fedora, etc.)

---

## 📚 موارد إضافية

- **[QUICKSTART.md](QUICKSTART.md)** - دليل البدء السريع
- **[README.md](README.md)** - معلومات عن النظام
- **[DEPLOYMENT.md](DEPLOYMENT.md)** - دليل النشر الكامل
- **[SECURITY.md](SECURITY.md)** - إرشادات الأمان

---

## 💡 نصائح للأداء الأفضل

1. **استخدم Express.js** للحصول على أفضل أداء
2. **أغلق التطبيقات الأخرى** التي قد تستخدم نفس المنفذ
3. **استخدم متصفح حديث** للأداء الأمثل
4. **للتطوير**: استخدم `npm run dev` للتحديث التلقائي
5. **تجنب التشغيل كـ root** (على Linux/Mac)

---

## 🎯 الخطوات السريعة (الطريقة الموصى بها)

```bash
# 1. تثبيت المتطلبات (مرة واحدة فقط)
npm install

# 2. تشغيل الخادم
npm start

# 3. افتح المتصفح على
# http://localhost:8080

# 4. استمتع بالنظام! 🎉
```

---

**تم إعداده بواسطة:** نظام إدارة المرور  
**التاريخ:** 2025  
**الترخيص:** MIT

جميع الحقوق محفوظة © 2025 - جامعة الإمام محمد بن سعود الإسلامية
