# Plate Recognizer Snapshot API Configuration
# إعدادات Plate Recognizer Snapshot API

# ============================================
# Plate Recognizer API Settings
# إعدادات Plate Recognizer API
# ============================================

# Your Plate Recognizer API Token
# Get it from: https://app.platerecognizer.com/accounts/api/
# احصل عليه من: https://app.platerecognizer.com/accounts/api/
PLATE_API_KEY=your_api_key_here

# Snapshot API URL (default is shown)
# رابط Snapshot API (القيمة الافتراضية موضحة)
SNAPSHOT_API_URL=https://api.platerecognizer.com/v1/plate-reader/

# ============================================
# PostgreSQL Database Configuration
# إعدادات قاعدة بيانات PostgreSQL
# ============================================

# Database connection URL
# Format: postgresql://user:password@host:port/database
# مثال: postgresql://username:password@localhost:5432/plate_recognizer
DATABASE_URL=postgresql://user:password@localhost:5432/plate_recognizer

# ============================================
# Processing Configuration
# إعدادات المعالجة
# ============================================

# Delay between API requests (in seconds) to avoid rate limiting
# تأخير بين طلبات API (بالثواني) لتجنب تجاوز الحد
REQUEST_DELAY=1.0

# Minimum confidence threshold (0.0 to 1.0)
# Images with confidence below this will be skipped
# عتبة الثقة الدنيا (من 0.0 إلى 1.0)
# سيتم تجاهل الصور ذات الثقة الأقل من هذه القيمة
CONFIDENCE_THRESHOLD=0.0

# Number of images to process before committing to database
# عدد الصور للمعالجة قبل الحفظ في قاعدة البيانات
BATCH_SIZE=10

# ============================================
# ⚠️ IMPORTANT SECURITY NOTES / ملاحظات أمنية مهمة
# ============================================

# 1. NEVER commit this file with real credentials to version control
#    لا تقم أبداً بإضافة هذا الملف مع بيانات حقيقية إلى نظام التحكم بالإصدار
#
# 2. Keep your API keys secure and rotate them regularly
#    احفظ مفاتيح API بشكل آمن وقم بتغييرها بشكل دوري
#
# 3. Use different API keys for development and production
#    استخدم مفاتيح API مختلفة للتطوير والإنتاج
#
# 4. Restrict database access with strong passwords
#    قيد الوصول إلى قاعدة البيانات بكلمات مرور قوية
#
# 5. STORAGE WARNING: Storing images as BYTEA in PostgreSQL will
#    significantly increase your database size. Consider using
#    external storage (S3, MinIO, etc.) for production systems.
#    تحذير التخزين: تخزين الصور كـ BYTEA في PostgreSQL
#    سيزيد حجم قاعدة البيانات بشكل كبير. فكر في استخدام
#    تخزين خارجي (S3، MinIO، إلخ) للأنظمة الإنتاجية.
#
# 6. PRIVACY WARNING: Storing vehicle images may have privacy
#    and legal implications. Ensure compliance with local laws
#    and regulations regarding data retention and privacy.
#    تحذير الخصوصية: تخزين صور المركبات قد يكون له آثار
#    خصوصية وقانونية. تأكد من الامتثال للقوانين المحلية
#    واللوائح المتعلقة بالاحتفاظ بالبيانات والخصوصية.

# ============================================
# Setup Instructions / تعليمات الإعداد
# ============================================

# 1. Copy this file to .env
#    انسخ هذا الملف إلى .env
#    $ cp .env.example .env
#
# 2. Fill in your actual values in .env
#    املأ قيمك الفعلية في .env
#
# 3. Install dependencies:
#    ثبت المتطلبات:
#    $ pip install -r snapshot_requirements.txt
#
# 4. Initialize database:
#    أنشئ قاعدة البيانات:
#    $ psql -U postgres -f db_schema.sql
#
# 5. Create images.txt with your image paths/URLs
#    أنشئ images.txt مع مسارات أو روابط الصور
#
# 6. Run the script:
#    شغّل السكربت:
#    $ python snapshot_to_postgres.py
