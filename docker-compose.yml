version: '3.8'

<<<<<<< HEAD
# Plate Recognizer Snapshot + PostgreSQL Stack
# نظام التعرف على اللوحات مع PostgreSQL

services:
  # PostgreSQL Database Service
  # خدمة قاعدة البيانات PostgreSQL
  db:
    image: postgres:15
    container_name: plate_recognizer_db
    restart: unless-stopped
    environment:
      POSTGRES_DB: vehicle_snapshots
      POSTGRES_USER: postgres
      # WARNING: Change this password for production use!
      # تحذير: غيّر كلمة المرور هذه للاستخدام في الإنتاج!
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres_password_change_me}
      PGDATA: /var/lib/postgresql/data/pgdata
    ports:
      - "5432:5432"
    volumes:
      # Persist database data
      # حفظ بيانات قاعدة البيانات
      - postgres_data:/var/lib/postgresql/data
      # Initialize schema on first run
      # تهيئة المخطط عند التشغيل الأول
      - ./db_schema.sql:/docker-entrypoint-initdb.d/01-schema.sql
    networks:
      - plate_recognizer_network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Plate Recognizer Application Service
  # خدمة تطبيق التعرف على اللوحات
  app:
    build:
      context: .
      dockerfile: Dockerfile.snapshot
    container_name: plate_recognizer_app
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    environment:
      # Plate Recognizer API Configuration
      # إعدادات API للتعرف على اللوحات
      PLATE_API_KEY: ${PLATE_API_KEY}
      SNAPSHOT_API_URL: ${SNAPSHOT_API_URL:-https://api.platerecognizer.com/v1/plate-reader/}
      
      # Database Configuration
      # إعدادات قاعدة البيانات
      # WARNING: Update password in .env for production!
      DATABASE_URL: postgresql://postgres:${POSTGRES_PASSWORD:-postgres_password_change_me}@db:5432/vehicle_snapshots
      
      # Request delay (seconds between API calls)
      # تأخير بين الطلبات (بالثواني)
      REQUEST_DELAY: ${REQUEST_DELAY:-1.0}
    volumes:
      # Mount images.txt file
      # تحميل ملف قائمة الصور
      - ./images.txt:/app/images.txt:ro
      # Optional: Mount local images directory
      # اختياري: تحميل مجلد الصور المحلية
      - ./input_images:/app/input_images:ro
    networks:
      - plate_recognizer_network
    # Command will be set in Dockerfile
    # الأمر سيتم تعيينه في Dockerfile

# Volumes for data persistence
# أحجام التخزين للحفاظ على البيانات
=======
services:
  # PostgreSQL Database
  # قاعدة بيانات PostgreSQL
  postgres:
    image: postgres:15-alpine
    container_name: traffic_postgres
    environment:
      POSTGRES_DB: ${DB_NAME:-traffic_system}
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-changeme}
      POSTGRES_INITDB_ARGS: "--encoding=UTF8"
    ports:
      - "${DB_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./db_schema.sql:/docker-entrypoint-initdb.d/01-schema.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-postgres} -d ${DB_NAME:-traffic_system}"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - traffic_network

  # Python Application for Plate Recognition
  # تطبيق Python للتعرف على اللوحات
  plate_recognizer:
    build:
      context: .
      dockerfile: Dockerfile.snapshot
    container_name: traffic_plate_recognizer
    environment:
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_NAME=${DB_NAME:-traffic_system}
      - DB_USER=${DB_USER:-postgres}
      - DB_PASSWORD=${DB_PASSWORD:-changeme}
      - PLATE_RECOGNIZER_API_TOKEN=${PLATE_RECOGNIZER_API_TOKEN}
      - PLATE_RECOGNIZER_API_URL=${PLATE_RECOGNIZER_API_URL:-https://api.platerecognizer.com/v1/plate-reader/}
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - ./snapshot_to_postgres.py:/app/snapshot_to_postgres.py:ro
      - ./images:/app/images:ro  # Mount for local images
    networks:
      - traffic_network
    # Keep container running for manual execution
    command: tail -f /dev/null

  # pgAdmin - PostgreSQL Administration Tool (Optional)
  # pgAdmin - أداة إدارة PostgreSQL (اختياري)
  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: traffic_pgadmin
    environment:
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_EMAIL:-admin@traffic.local}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_PASSWORD:-admin}
      PGADMIN_LISTEN_PORT: 80
    ports:
      - "5050:80"
    depends_on:
      - postgres
    networks:
      - traffic_network
    restart: unless-stopped
    profiles:
      - tools  # Only start with: docker-compose --profile tools up

>>>>>>> origin/main
volumes:
  postgres_data:
    driver: local

<<<<<<< HEAD
# Network for inter-service communication
# الشبكة للاتصال بين الخدمات
networks:
  plate_recognizer_network:
    driver: bridge
=======
networks:
  traffic_network:
    driver: bridge

# ============================================
# Important Notes / ملاحظات مهمة
# ============================================
#
# 1. Storage Considerations / اعتبارات التخزين:
#    - Images stored in PostgreSQL will consume significant disk space
#    - الصور المخزنة في PostgreSQL ستستهلك مساحة كبيرة من القرص
#    - Monitor volume size: docker system df -v
#    - راقب حجم الحجم: docker system df -v
#
# 2. Security / الأمان:
#    - Change default passwords in .env file
#    - غيّر كلمات المرور الافتراضية في ملف .env
#    - Never commit .env file to repository
#    - لا تضف ملف .env إلى المستودع أبداً
#
# 3. Usage / الاستخدام:
#    - Start services: docker-compose up -d
#    - تشغيل الخدمات: docker-compose up -d
#    - Run plate recognition: docker-compose exec plate_recognizer python snapshot_to_postgres.py <image_source>
#    - تشغيل التعرف على اللوحات: docker-compose exec plate_recognizer python snapshot_to_postgres.py <image_source>
#    - View logs: docker-compose logs -f
#    - عرض السجلات: docker-compose logs -f
#
# 4. Backup / النسخ الاحتياطي:
#    - Backup database: docker-compose exec postgres pg_dump -U postgres traffic_system > backup.sql
#    - النسخ الاحتياطي: docker-compose exec postgres pg_dump -U postgres traffic_system > backup.sql
#
# 5. pgAdmin (Optional Tool):
#    - Start with tools: docker-compose --profile tools up -d
#    - Access at: http://localhost:5050
#    - الوصول على: http://localhost:5050
>>>>>>> origin/main
